# 2026-04-08

## Summary
- Audited the inconsistent admin top headers across `src/frontend-vi/admin/`
- Moved the admin top header into the shared injector at `src/frontend-vi/shared/nav-components.js`
- Standardized the admin chrome so all non-login admin screens now share one top-header structure

## Implementation Notes
- Added `ADMIN_TOPBAR_CONFIG` keyed by admin filename
- Introduced a common top-header first row with:
  - search
  - notifications
  - help
  - settings
  - profile block
- Added optional secondary rows for screens that previously depended on custom top-header content:
  - `04-kitchen-display-system-kds.html`
  - `07-table-management---floor-plan-view.html`
  - `08-order-management.html`
  - `10-menu-management-list.html`
- Updated the admin injection flow to remove legacy page-level `<header>` blocks before inserting the shared top header

## Verification
- `node --check src/frontend-vi/shared/nav-components.js` passed
- Static coverage check confirmed that every non-login admin HTML file is represented in the shared topbar config
- A lightweight Node DOM simulation confirmed:
  - the shared header is injected
  - the main admin canvas still receives `margin-left: 256px`
  - representative secondary rows appear for KDS, order management, and menu management

## Residual Risk
- The legacy per-page top-header markup still exists in source HTML and is now removed at runtime
- If later submission hygiene requires cleaner source files, the old header blocks can be deleted in a dedicated cleanup pass without changing the runtime UI

## Follow-up Session - English Admin Cleanup

### Summary
- Audited and corrected the English admin mockups under `src/frontend/admin/`
- Removed the destructive admin-shell rewrite path from `src/frontend/shared/nav-components.js`
- Standardized the English admin HTML scaffold, centralized Tailwind admin tokens, and cleaned the visible mojibake in the admin UI text

### Implementation Notes
- Replaced the old admin runtime flow that:
  - removed all sidebars
  - flattened wrapper containers
  - rewrote existing headers
  - hid headings/search bars heuristically
- Changed the shared admin runtime so it now:
  - preserves authored admin headers and content
  - reuses an authored sidebar when one exists
  - only injects missing shell pieces
- Added `src/frontend/shared/admin-tailwind-config.js` as the shared Tailwind token source for all English admin HTML files
- Removed the bespoke authored sidebars from the English admin outlier screens and left them on the shared injected sidebar path instead:
  - `02-process-payment---order-wnt-032.html`
  - `04-kitchen-display-system-kds.html`
  - `05-staff-management-list.html`
  - `11-add-new-staff-member-form.html`
- Normalized all `src/frontend/admin/*.html` files onto a valid `<!DOCTYPE html>` plus `head/body` scaffold
- Replaced inline page-level `tailwind.config` blocks with the shared config include
- Normalized the repeated `w-[calc(100%-16rem)]` utility to an explicit shared form: `w-[calc(100%_-_16rem)]`
- Cleaned the visible broken currency/dash encoding in the English admin screens and normalized currency display to `VND`

### Verification
- `node --check src/frontend/shared/nav-components.js` passed
- `14` English admin HTML files now include `../shared/admin-tailwind-config.js`
- Static scan for old admin scaffold/config patterns returned `0` matches for:
  - `<head></head><body`
  - `<script id="tailwind-config">`
  - `w-[calc(100%-16rem)]`
- Static scan of `src/frontend/shared/nav-components.js` returned no matches for the removed destructive logic markers:
  - `cleanupAdminContent`
  - `allAsides`
  - `existingHeaders.forEach(`
  - `header.innerHTML =`
  - `body > div.flex`
- Targeted raw-string scan returned `0` matches for the previously visible English admin mojibake markers

### Residual Risk
- The English admin screens now share a cleaner baseline shell/token/document structure, but they are still static mockups with per-screen visual differences in density and layout polish
- If the next phase wants a truly unified admin look, the remaining work is visual harmonization by screen rather than more shell/runtime cleanup

## Follow-up Session - English Admin Layout Audit

### Summary
- Performed a deep page-by-page layout audit across all `14` English admin HTML screens
- Classified the admin pages into two shell contracts:
  - `header-inside-main`
  - `header-before-main`
- Applied the first structural remediation pass to reduce obvious layout drift before deeper visual harmonization

### Implementation Notes
- Audited admin layout against:
  - shell contract
  - outer/inner canvas spacing
  - content hierarchy
  - overflow and scroll layering
  - workflow split-pane density
- Reduced the shared outer `main` canvas padding on the English admin screens from `p-8` to `p-6`
- Fixed the broken scaffold in `src/frontend/admin/06-assign-order-to-table.html` where the authored `<header>` had been emitted inside `<head>`
- Normalized the `header-inside-main` English admin pages so the inner header now uses `w-full` instead of carrying a second sidebar offset:
  - `02-process-payment---order-wnt-032.html`
  - `04-kitchen-display-system-kds.html`
  - `05-staff-management-list.html`
  - `10-menu-management-list.html`
  - `11-add-new-staff-member-form.html`
  - `13-admin-dashboard-overview.html`
  - `14-revenue-statistics-reports.html`

### Verification
- Static scan confirmed no English admin file now emits a page `<header>` before `</head>`
- Static scan confirmed the `header-inside-main` pages above now use `w-full` headers
- The remaining pages with `ml-64 w-[calc(100%_-_16rem)]` headers are all part of the older `header-before-main` contract and were intentionally left for a separate normalization pass:
  - `03-admin-notifications-overview.html`
  - `06-assign-order-to-table.html`
  - `07-table-management---floor-plan-view.html`
  - `08-order-management.html`
  - `09-create-in-store-order.html`
  - `12-add-new-menu-item-form.html`

### Residual Risk
- The English admin shell is still split across two layout contracts, so spacing/alignment drift remains between sibling-header pages and header-inside-main pages
- Workflow-heavy pages (`06`, `07`, `08`, `09`) still use rigid split-pane widths and overflow patterns that need a dedicated visual normalization pass

## Follow-up Session - English Admin Layout Remediation

### Summary
- Continued the admin HTML cleanup after the audit by rebalancing workflow-heavy layouts and tightening several over-spaced supporting pages
- Kept the current sibling-header pages on their existing shell contract for safety, while improving their content canvases and split-pane behavior directly in HTML

### Implementation Notes
- Adjusted `03-admin-notifications-overview.html` to:
  - widen the main content wrapper
  - reduce oversized vertical spacing
  - allow filter tabs to scroll horizontally instead of clipping
- Adjusted `06-assign-order-to-table.html` to:
  - add safer inter-panel spacing
  - soften the rigid 50/50 split
  - reduce panel padding
  - make the floor grid less dense at non-wide desktop sizes
- Adjusted `07-table-management---floor-plan-view.html` to:
  - reduce canvas padding and inter-table gaps
  - tighten the right-side panel width
  - reduce left-panel overflow pressure with `min-w-0`
- Adjusted `08-order-management.html` to:
  - switch the list/detail split to flex ratios instead of fixed width percentages
  - add horizontal resilience to the filter row
  - turn both panels into more clearly bounded surfaces
- Adjusted `09-create-in-store-order.html` to:
  - give the content panel a `min-w-0` contract
  - make the menu grid less rigid
  - reduce the fixed width of the sticky basket
- Adjusted `12-add-new-menu-item-form.html` to:
  - center and widen the content wrapper
  - reduce the oversized title
  - make the form and image grid more adaptive
- Adjusted `01-wonton-pos-admin-login.html` to:
  - remove the fixed footer overlap risk
  - slightly tighten the vertical rhythm
  - replace the broken password placeholder text with a clean label
- Adjusted `11-add-new-staff-member-form.html` to:
  - reduce the oversized outer wrapper padding
  - tighten the form-card vertical rhythm

### Verification
- Static scan confirmed the old fixed-width percentage pair in `08-order-management.html` is gone
- Static scan confirmed the new wrapper/split classes are present in:
  - `03-admin-notifications-overview.html`
  - `06-assign-order-to-table.html`
  - `07-table-management---floor-plan-view.html`
  - `08-order-management.html`
  - `09-create-in-store-order.html`
  - `12-add-new-menu-item-form.html`
- Static scan confirmed `01-wonton-pos-admin-login.html` now uses:
  - a non-fixed footer
  - a clean `Enter password` placeholder

### Residual Risk
- The remaining `header-before-main` pages still share a second shell pattern and should be normalized in a dedicated pass if the goal is a single admin shell contract
- The current verification is still HTML/static-class based; a browser pass is still needed to validate exact visual balance at representative desktop widths
