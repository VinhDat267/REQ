# Journal - 2026-04-18

## Mermaid PFD Canonical Format Change
- Accepted the user's decision that Process Flow Diagrams are closer to flowcharts and should use Mermaid instead of PlantUML.
- Added ADR `039-use-mermaid-for-process-flow-diagrams.md`.
- Updated the rule layer so:
  - UML Activity Diagrams, use-case diagrams, ERDs, and other UML-style diagrams remain PlantUML-first
  - Process Flow Diagrams are now Mermaid `flowchart` artifacts
  - PFD sources belong under `docs/diagrams/mermaid/process-flow/`
  - PFD rendered assets belong under `docs/diagrams/mermaid/process-flow/rendered/`
- Created Mermaid PFD sources and rendered SVG/PNG outputs for:
  - `UC-17_ReorderPastOrder_PFD`
  - `UC-18_ManagePromotions_PFD`
  - `UC-25_ManageCustomerAccountAndAuthentication_PFD`
- Rendered the Mermaid PFDs with Mermaid CLI `11.12.0` via `npx @mermaid-js/mermaid-cli`.

## Mermaid PFD Word-Compatible White Exports
- Rerendered the current Mermaid PFD SVG/PNG asset set with a white background after the user reported that SVG imports into Microsoft Word lost text.
- Confirmed the PNG exports have an opaque white top-left pixel (`R255 G255 B255 A255`) and are the safest format for inserting into Word.
- Verified the generated SVGs still contain Mermaid `foreignObject` labels, which explains why Word can drop visible text; keep the SVGs for browser/vector reference, but prefer the white PNG outputs for Word deliverables.
- Exported higher-resolution Word insertion PNGs with `_word.png` suffix at Mermaid CLI scale 4 for the current PFD set, because the normal PNG exports can look blurry when enlarged in Microsoft Word.

## UC-19 AD and PFD
- Created `UC-19: View Operational Dashboard` diagrams for the current BRD final v3 lane.
- Added the PlantUML Activity Diagram source at `docs/diagrams/plantuml/activity/UC-19_ViewOperationalDashboard.puml` and rendered SVG/PNG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Added the Mermaid Process Flow Diagram source at `docs/diagrams/mermaid/process-flow/UC-19_ViewOperationalDashboard_PFD.mmd` and rendered white-background SVG/PNG outputs under `docs/diagrams/mermaid/process-flow/rendered/`.
- Verified there were no PlantUML/Mermaid syntax-error markers in the rendered SVGs and confirmed both PNG outputs have opaque white backgrounds.

## UC-20 AD and PFD
- Created `UC-20: Manage Active Orders` diagrams for the current BRD final v3 lane.
- Added the PlantUML Activity Diagram source at `docs/diagrams/plantuml/activity/UC-20_ManageActiveOrders.puml` and rendered SVG/PNG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Added the Mermaid Process Flow Diagram source at `docs/diagrams/mermaid/process-flow/UC-20_ManageActiveOrders_PFD.mmd` and rendered white-background SVG/PNG outputs under `docs/diagrams/mermaid/process-flow/rendered/`.
- Covered the required branches from the UC spec: filtering, terminal-state rejection, confirmation, unpaid cancellation, paid cancellation/refund escalation, table/order transfer, overdue Pickup/Takeaway routing, Completed+Unpaid settlement follow-up, and Manager escalation for operational exceptions.
- Verified there were no PlantUML/Mermaid syntax-error markers in the rendered SVGs and confirmed both PNG outputs have opaque white backgrounds.

## UC-21 AD and PFD
- Created `UC-21: Close Shift and Reconcile End-of-day` diagrams for the current BRD final v3 lane.
- Added the PlantUML Activity Diagram source at `docs/diagrams/plantuml/activity/UC-21_CloseShiftAndReconcileEndOfDay.puml` and rendered SVG/PNG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Added the Mermaid Process Flow Diagram source at `docs/diagrams/mermaid/process-flow/UC-21_CloseShiftAndReconcileEndOfDay_PFD.mmd` and rendered white-background SVG/PNG outputs under `docs/diagrams/mermaid/process-flow/rendered/`, including a high-resolution `_word.png` export.
- Covered the required branches from the UC spec and shift-close rules: open-shift validation, cash count, hardware/connectivity manual count flag, variance threshold and Manager approval, unresolved-item review, Refund Pending / inherited exception handling, shift report locking, end-of-day aggregation, and Manager reopen with audit reason.
- Verified there were no PlantUML/Mermaid syntax-error markers in the rendered SVGs and confirmed PNG outputs have opaque white backgrounds.

## UC-23 AD and PFD
- Created `UC-23: Serve and Confirm Handoff` diagrams for the current BRD final v3 lane.
- Added the PlantUML Activity Diagram source at `docs/diagrams/plantuml/activity/UC-23_ServeAndConfirmHandoff.puml` and rendered SVG/PNG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Added the Mermaid Process Flow Diagram source at `docs/diagrams/mermaid/process-flow/UC-23_ServeAndConfirmHandoff_PFD.mmd` and rendered white-background SVG/PNG outputs under `docs/diagrams/mermaid/process-flow/rendered/`, including a high-resolution `_word.png` export.
- Covered the required branches from the UC spec and operational rules: Ready queue, correct Dine-in table delivery, Takeaway/Pickup order-code matching, multiple Ready orders for the same table/customer, missing/wrong item routing to `UC-24`, overdue/no-show handling without auto-completion, manual handoff note during connectivity loss, and same-shift wrong-handoff correction with audit reason.
- Verified there were no PlantUML/Mermaid syntax-error markers in the rendered SVGs and confirmed PNG outputs have opaque white backgrounds.

## UC-24 AD and PFD
- Created `UC-24: Handle Complaint and Operational Exception` diagrams for the current BRD final v3 lane.
- Added the PlantUML Activity Diagram source at `docs/diagrams/plantuml/activity/UC-24_HandleComplaintAndOperationalException.puml` and rendered SVG/PNG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Added the Mermaid Process Flow Diagram source at `docs/diagrams/mermaid/process-flow/UC-24_HandleComplaintAndOperationalException_PFD.mmd` and rendered white-background SVG/PNG outputs under `docs/diagrams/mermaid/process-flow/rendered/`, including a high-resolution `_word.png` export.
- Covered the required branches from the UC spec and operational rules: complaint intake, issue category selection, anonymized guest PII handling, terminal `Refunded` guard with new request / Manager justification, remake and substitution without fake revenue, goodwill discount reason, partial/full refund with `Refund Pending`, full-order `Comp`, Dine-in `Write-off`, Manager approval rejection paths, audit trail, and shift-close reconciliation effects.
- Verified there were no PlantUML/Mermaid syntax-error markers in the rendered SVGs and confirmed PNG outputs have opaque white backgrounds.

## UC-26 AD and PFD
- Created `UC-26: Complete Checkout and Apply Promotion` diagrams for the current BRD final v3 lane.
- Added the PlantUML Activity Diagram source at `docs/diagrams/plantuml/activity/UC-26_CompleteCheckoutAndApplyPromotion.puml` and rendered SVG/PNG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Added the Mermaid Process Flow Diagram source at `docs/diagrams/mermaid/process-flow/UC-26_CompleteCheckoutAndApplyPromotion_PFD.mmd` and rendered white-background SVG/PNG outputs under `docs/diagrams/mermaid/process-flow/rendered/`, including a high-resolution `_word.png` export.
- Covered the required branches from the UC spec and business rules: empty/unavailable cart rejection, supported service model check, Pickup slot/capacity validation through `UC-03`, optional promotion entry, no-stacking rule, promotion validity / usage-limit / applicability checks, invalid promotion notice with failed-attempt logging, authenticated profile confirmation, Guest Name + Phone capture, order creation, Takeaway/Pickup routing to `UC-02`, and Dine-in routing to `UC-11`.
- Verified there were no PlantUML/Mermaid syntax-error markers in the rendered SVGs and confirmed PNG outputs have opaque white backgrounds.

## FinalV3 ERD Overview Simplification
- Added `docs/diagrams/plantuml/erd/WontonPOS_FinalV3_ERD_Overview.puml` as a clean presentation/review ERD for the Final BRD v3 26-UC lane.
- Preserved `docs/diagrams/plantuml/erd/WontonPOS_FinalV3_ERD.puml` and `docs/diagrams/erd/WontonPOS_FinalV3_Schema.sql` as the detailed field-level source truth.
- Reduced visual clutter by showing only core entities and relationships, omitting relationship labels, and folding support tables into a bottom legend.
- Folded hidden details include password reset, staff role junctions, menu/topping junctions, order status history, promotion usage, shift reconciliation buckets, cash drawer movements, inventory adjustment detail, and noisy audit links.
- Rendered the overview through PlantUML server into `docs/diagrams/plantuml/erd/rendered/WontonPOS_FinalV3_ERD_Overview.svg` and `.png`, plus a high-resolution `WontonPOS_FinalV3_ERD_Overview_word.png` export for Word insertion.
- Verified the rendered SVG contains no `Syntax Error`, `ERROR`, or `class="error"` markers; the normal PNG dimensions are `968x1164` and the Word PNG dimensions are `2906x3493`.

## FinalV3 Class Diagram
- Created class diagram deliverables for the current Final BRD v3 `UC-01..UC-26` lane from `docs/diagrams/erd/WontonPOS_FinalV3_Schema.sql`, the FinalV3 ERD, and the approved 26-UC shortlist.
- Added PlantUML detailed and compact class diagram sources under `docs/diagrams/plantuml/class/`, with rendered SVG/PNG outputs under `docs/diagrams/plantuml/class/rendered/`.
- Added the user-requested Mermaid class diagram source at `docs/diagrams/mermaid/class/WontonPOS_FinalV3_ClassDiagram.mmd`, with white-background SVG/PNG outputs and a high-resolution `WontonPOS_FinalV3_ClassDiagram_word.png` export under `docs/diagrams/mermaid/class/rendered/`.
- Verified the Mermaid source with Mermaid CLI `11.12.0` through `npx @mermaid-js/mermaid-cli`; the rendered SVG contains no syntax/error markers.
- Note: the Mermaid class diagram is a requested presentation/source variant; PlantUML remains the repo's default canonical format for UML-style diagrams unless a later ADR changes that policy.

## FinalV3 Mermaid Class Diagram Review Fix
- Reviewed the Mermaid class diagram against the current `UC-01..UC-26` specifications and `WontonPOS_FinalV3_Schema.sql`.
- Fixed the Mermaid source so it is no longer only a compact overview: added `PasswordResetToken`, `StaffRole`, `OrderItemTopping`, `MenuItemAvailabilityEvent`, `StockAdjustment`, `ShiftInheritedException`, `CashDrawerMovement`, and domain enum classes for service model, order/payment/item status, payment method, refund status, discount type, table status, shift status, and complaint resolution.
- Added the missing relationships needed to defend coverage for `UC-17`, `UC-21`, `UC-22`, `UC-25`, and `UC-26`, including topping snapshots, 86'd audit events, stock adjustments, inherited shift exceptions, cash drawer movements, password reset tokens, and role assignment.
- Rerendered the Mermaid SVG, normal PNG, and high-resolution Word PNG with white backgrounds via Mermaid CLI; validation found no syntax/error markers in the rendered SVG.

## FinalV3 Mermaid Class Diagram Simplification
- Simplified the Mermaid class diagram after user feedback that the full coverage version was visually cluttered.
- Replaced the expanded support-class/enum rendering with an `11`-class presentation model: `Customer`, `Staff`, `MenuCatalog`, `DiningTable`, `Order`, `OrderLine`, `Promotion`, `PaymentRecord`, `OperationsControl`, `CustomerEngagement`, and `AuditLog`.
- Folded password reset, staff roles, category/item/topping, 86'd availability, stock adjustment, pickup slot capacity, payment/refund/cash movement, shift reconciliation, complaints, exceptions, notifications, and reviews into aggregate fields and source comments.
- Reduced visible relationships to `12` core links and removed multiplicity labels that were creating line clutter.
- Rerendered the Mermaid SVG, normal PNG, and high-resolution Word PNG with white backgrounds. The compact PNG is `784x927`, and the Word PNG is `3136x3708`; validation found no syntax/error markers in the rendered SVG.

## FinalV3 Mermaid Class Diagram Balanced Version
- Rebalanced the Mermaid class diagram after follow-up feedback that the `11`-class version was too sparse.
- Restored expected visible domain concepts without reintroducing enum/support-table clutter: `Role`, `MenuCategory`, `MenuItem`, `Topping`, `PickupSlotConfig`, `PaymentTransaction`, `Refund`, `Shift`, `Complaint`, `Notification`, and `OrderReview`.
- Kept detailed support concepts folded into attributes/comments rather than standalone classes, including password reset, staff-role junction, order-item topping junction, 86'd audit, stock adjustment, shift inherited exceptions, and cash drawer movements.
- Rerendered the Mermaid SVG, normal PNG, and high-resolution Word PNG with white backgrounds. The balanced source now has `18` visible classes and `23` relationships; the PNG is `784x1321`, and the Word PNG is `3136x5284`; validation found no syntax/error markers in the rendered SVG.

## FinalV3 Mermaid Class Diagram Medium-Detail Version
- Added a small amount of detail to the balanced Mermaid class diagram without returning to the earlier cluttered enum/support-table version.
- Restored five focused support classes that are useful for 26-UC review defense: `PasswordResetToken`, `MenuAvailabilityEvent`, `OrderStatusHistory`, `PromotionUsage`, and `ShiftReconciliationBucket`.
- Removed visible multiplicity labels from relationship lines to keep the medium-detail version readable after adding the extra classes.
- Rerendered the Mermaid SVG, normal PNG, and high-resolution Word PNG with white backgrounds. The current source now has `23` visible classes and `29` relationships; the PNG is `784x1440`, and the Word PNG is `3136x5760`; validation found no syntax/error markers in the rendered SVG.

## FinalV3 Mermaid Class Diagram Connector Cleanup
- Cleaned up connector clutter in the medium-detail Mermaid class diagram after review feedback that the relationship lines were hard to follow.
- Kept the same `23` visible classes but reduced visible relationships from `29` to `18` by removing actor/report/reconciliation detail lines that can be represented through attributes, comments, or `AuditLog`.
- Shortened several relationship labels and restored `direction LR` after a `direction TB` test made the diagram too wide and small for Word.
- Rerendered the Mermaid SVG, normal PNG, and high-resolution Word PNG with white backgrounds. The connector-clean PNG is `784x1102`, and the Word PNG is `3136x4408`; validation found no syntax/error markers in the rendered SVG.

## FinalV3 Use Case Diagram Split and Connector Cleanup
- Continued the user-requested split of the Final BRD v3 use case diagram into separate Client and Admin views.
- Confirmed the split uses the current `26`-UC final BRD lane: Client has `10` customer-facing UCs and Admin has `16` manager / FOH / BOH UCs.
- Updated the PlantUML full, Admin, and Client use-case sources to use straight polyline connectors instead of curved spline connectors.
- Updated the Mermaid use-case presentation sources to use linear connectors instead of curved `basis` connectors.
- Rerendered the PlantUML full, Admin, and Client UCD PNG/SVG assets successfully.

## FinalV3 Use Case Diagram Actor Link Straightening
- Applied the user's stricter diagram rule that actor-to-use-case associations must be one straight line segment, not merely non-curved polylines.
- Added `scripts/straighten_usecase_actor_links.py` to post-process rendered PlantUML SVG files by replacing actor-to-UC association paths with direct `M x,y L x,y` paths.
- Ran the post-processor on the full, Admin, and Client UCD SVGs:
  - full UCD: `30` actor-to-use-case links straightened
  - Admin UCD: `20` actor-to-use-case links straightened
  - Client UCD: `10` actor-to-use-case links straightened
- Re-exported the corrected SVGs to white-background PNGs with `resvg-js`, and visually confirmed the Admin and Client PNGs now show actor associations as single straight segments.

## FinalV3 Use Case Diagram Native Routing Restore
- Followed the user's later preference that the UCD connector lines do not need to be perfectly straight and may be drawn like the earlier/native layout.
- Removed the UCD `skinparam linetype polyline` setting and the straightening render note from the full, Admin, and Client PlantUML use-case sources.
- Restored the Mermaid use-case presentation curve setting from `linear` back to `basis`.
- Removed the now-abandoned `scripts/straighten_usecase_actor_links.py` helper.
- Rerendered the PlantUML full, Admin, and Client UCD SVG/PNG assets directly from PlantUML without SVG post-processing.

## FinalV3 Use Case Diagram 26-UC Review Fix
- Fixed the UCD actor mapping after reviewing the diagrams against `UC_Specifications_26_EN.md` and the current 26-UC shortlist.
- Moved `UC-25: Manage Customer Account and Authentication` from `Registered Customer` to the generic `Customer` actor because the UC includes registration, login, password recovery, and profile lifecycle rather than only already-registered actions.
- Split the staff side into generic `FOH Staff` plus specialized `Cashier` and `Service Staff (Waiter)` actors, with both modeled as specializations of `FOH Staff`.
- Kept `Cashier` on `UC-10`, `UC-21`, and pickup/takeaway handoff responsibility in `UC-23`; kept `Service Staff (Waiter)` on dine-in handoff responsibility in `UC-23`.
- Added `UC-24` extension links from `UC-20` and `UC-22` in addition to the existing handoff exception link from `UC-23`.
- Synced the Mermaid use-case presentation sources with the PlantUML UCD semantics and validated both Mermaid sources through Mermaid CLI.
- Rerendered the PlantUML full, Admin, and Client UCD SVG/PNG assets successfully.

## FinalV3 Draw.io Use Case Diagram
- Created `docs/diagrams/drawio/use-case/WontonPOS_FinalV3_UseCase.drawio` from the current Final BRD v3 26-UC UCD semantics.
- Matched the provided Draw.io reference style with UML actor shapes, ellipse use cases, a locked system boundary, Times New Roman text styling, and orthogonal Draw.io connectors.
- Included three pages in one workbook: `Full 26 UC`, `Client WebApp`, and `Admin WebApp`.
- Kept the corrected staff actor naming as `Service Staff (Waiter)` rather than `Server`.
- Added `scripts/generate_finalv3_ucd_drawio.py` so the Draw.io workbook can be regenerated consistently from the curated 26-UC layout data.
- Verified the generated `.drawio` XML parses successfully and contains the expected three pages.

## FinalV3 Draw.io UCD Layout Revert
- Reverted the later Draw.io layout cleanup at user request and regenerated `docs/diagrams/drawio/use-case/WontonPOS_FinalV3_UseCase.drawio` back to the first generated arrangement so the user can adjust it manually in Draw.io.
- Kept the same three pages, 26-UC content, actor naming, and include / extend relationships.
- Revalidated the regenerated workbook XML and confirmed there is no `Server` actor label.

## FinalV3 Draw.io Include Extend Label Fix
- Fixed Draw.io edge labels for `<<include>>` and `<<extend>>`.
- The first generated workbook stored the labels as raw angle-bracket text on HTML-enabled edge cells, which Draw.io rendered as an empty tag-like marker instead of visible stereotype text.
- Updated `scripts/generate_finalv3_ucd_drawio.py` to emit escaped label values (`&lt;&lt;include&gt;&gt;`, `&lt;&lt;extend&gt;&gt;`) so Draw.io displays the text correctly.
- Regenerated and revalidated `docs/diagrams/drawio/use-case/WontonPOS_FinalV3_UseCase.drawio`.

## Final SRS 26-UC Specification Completion
- Reviewed `docs/srs/SRSReport_Class01_Group01.docx` against the current final-project scope, `BRD_Final_V3_UC_Shortlist.md`, and the repo convention that the visible final SRS/BRD lane should use the curated `UC-01..UC-26` set.
- Found the SRS was already broadly aligned with the locked final scope in the introduction, scope, FR/NFR, user roles, constraints, UCD explanation, ERD explanation, and class diagram explanation, but section `2.8 USE CASE MODEL DESCRIPTION` only contained `9` UC specification tables.
- Rebuilt section `2.8` in the same DOCX so it now contains `26` use case specification tables ordered from `UC-01` through `UC-26`, preserving the existing 10-row table format (`ID & Name`, actor, description, trigger, pre/post conditions, normal flow, alternatives, exceptions, priority).
- Kept the business baseline locked to `Dine-in`, `Takeaway`, and `Pickup`, with `Delivery` still only mentioned as an out-of-scope extension.
- Verified the updated DOCX with `python-docx`: `26` UC tables, each `10x2`, no missing curated final-lane UC, and the next section remains `2.9.1 ENTITY RELATIONSHIP DIAGRAM (ERD)`.
- Created a safety backup at `tmp/docs/SRSReport_Class01_Group01.backup_pre_26uc.docx`; full page-layout rendering remains unverified because LibreOffice/Poppler/Word automation tools are not available in the current environment.

## Final SRS Client/Admin UCD Description Refresh
- Parsed the user-replaced detailed Draw.io SVG exports for `Client WebApp` and `Admin WebApp`, plus the three-page `WontonPOS_FinalV3_UseCase.drawio` workbook, to recover the visible actors, packages, use cases, and include/extend/generalization semantics.
- Rewrote section `2.7 USE CASE DIAGRAM` in `docs/srs/SRSReport_Class01_Group01.docx` so the two detailed UCD images are followed by fresh explanatory paragraphs matching the new diagrams.
- The Client WebApp explanation now covers `Customer (Guest / Registered)`, `Registered Customer`, the three functional packages, account/authentication, online order, pickup scheduling, checkout/promotion, online payment, order history, reorder, tracking, rating, and notification relationships.
- The Admin WebApp explanation now covers `FOH Staff`, `Cashier`, `Service Staff (Waiter)`, `Manager`, `BOH Staff (Kitchen)`, the three internal operation packages, table assignment/payment extensions, active-order management, service handoff, complaint/exception extensions, manager controls, shift reconciliation, kitchen status updates, and `86'd` menu handling.
- Renumbered the later SRS figure captions so the ERD is now `Fig 3` and the Class Diagram is now `Fig 4`, avoiding collision with the combined Client/Admin UCD caption.
- Synced the section `2.8` intro and modelling-assumption text so they reference the detailed Client/Admin UCD views rather than the older single overall UCD wording.
- Verified the DOCX structurally with `python-docx`: the three target captions exist once each, the stale `Fig 3. Class Diagram` caption is gone, and the old `one overall Use Case Diagram` wording no longer appears.
- Created a safety backup at `tmp/docs/SRSReport_Class01_Group01.backup_pre_ucd_description.docx`; visual page-layout rendering remains unverified because LibreOffice/Poppler/Word automation tools are unavailable in the current environment.

## Final SRS Section 1.3 Glossary Expansion
- Expanded `docs/srs/SRSReport_Class01_Group01.docx` section `1.3 Definitions, Acronyms, and Abbreviations` from a short acronym list into a final-scope glossary.
- Preserved the existing bullet-line format while adding project-specific and technical terms for `UC`, `UCD`, `UML`, `HTML5`, `CSS3`, `RAM`, `Client WebApp`, `Admin WebApp`, `Guest Customer`, `Registered Customer`, `Dine-in`, `Takeaway`, `Pickup`, `Delivery`, `Checkout`, `Order Code`, `Order Status`, `Payment Status`, `Payment Gateway`, `Refund Pending`, `Write-off`, `Comp`, `Forfeited`, `86'd`, `FIFO`, `Handoff`, `Promotion`, `Reconciliation`, and `Operational Exception`.
- Updated the heading from `Definition, Acronyms, Abbreviation` to `Definitions, Acronyms, and Abbreviations` for correct academic wording.
- Verified the DOCX structurally with `python-docx`: section `1.3` now has `44` glossary entries, every entry starts with the correct `●` bullet, there are no corrupted `?` bullet prefixes, and the next heading remains `2.   OVERALL DESCRIPTION`.
- Created a safety backup at `tmp/docs/SRSReport_Class01_Group01.backup_pre_13_definitions.docx`; visual page-layout rendering remains unverified because LibreOffice/Poppler/Word automation tools are unavailable in the current environment.

## Final SRS Functional Requirement Traceability Fix
- Rewrote `docs/srs/SRSReport_Class01_Group01.docx` section `2.4.1 FUNCTIONAL REQUIREMENTS` after review found the earlier `22` broad FRs were semantically valid but weaker for final grading traceability against the curated `UC-01..UC-26` lane.
- Replaced the section with `26` numbered requirements (`R.1..R.26`), each explicitly mapped in the title to its corresponding use case (`UC-01..UC-26`).
- Preserved the existing paragraph format for every requirement: title line plus `Description`, `Input`, `Output`, and `Processing`.
- Split previously grouped coverage into clearer FRs for `View Order History`, `Rate Order`, `Reorder Past Order`, `Track Order`, `Receive Order Notifications`, dashboard/statistics, active-order handling, 86'd availability, handoff, complaint/exception handling, account/authentication, and checkout/promotion.
- Corrected the earlier staff-authentication ambiguity by keeping customer registration/password flows in `UC-25` and staff account creation/permission management in `UC-06`.
- Strengthened final business-rule coverage in the FR wording: Takeaway/Pickup prepayment before kitchen intake, separate order/payment statuses, duplicate/late payment callback handling, guest tracking by `Order Code + Phone Number`, manager approval for sensitive changes, audit reasons, 86'd blocking of new checkout, wrong-handoff correction, refund/write-off/comp/forfeited handling, and shift reconciliation.
- Verified the DOCX structurally with `python-docx`: section `2.4.1` now has `26` FR titles, FR numbering is sequential `R.1..R.26`, title mappings are sequential `UC-01..UC-26`, each field type appears `26` times, no curly `86’d` remains in the FR section, and the next heading remains `2.4.2  NON FUNCTIONAL REQUIREMENTS`.
- Created a safety backup at `tmp/docs/SRSReport_Class01_Group01.backup_pre_functional_requirements_fix.docx`; visual page-layout rendering remains unverified because LibreOffice/Poppler/Word automation tools are unavailable in the current environment.

## Final SRS Readiness Polish
- Added the missing parent heading `2.4 REQUIREMENTS` before `2.4.1 FUNCTIONAL REQUIREMENTS` so the section numbering no longer jumps directly from `2.3` to `2.4.1`.
- Repaired a corrupted bullet in section `2.6 CONSTRAINTS` where the modelling-types constraint had been displayed with a `?` prefix instead of the correct `●` bullet.
- Replaced stale assumption wording in section `2.10 ASSUMPTIONS AND DEPENDENCIES` that still referred to a `final overall Use Case Diagram`; the assumption now correctly refers to detailed Client/Admin UCD views, the overall ERD, and the overall Class Diagram.
- Verified the DOCX structurally after this polish pass: no stale `final overall Use Case Diagram` wording remains, no corrupted `?` bullet prefix remains, the FR count remains `26`, each FR field count remains `26`, and the use case specification table count remains `26`.
- Created a safety backup at `tmp/docs/SRSReport_Class01_Group01.backup_pre_final_srs_polish.docx`; visual page-layout rendering remains unverified because LibreOffice/Poppler/Word automation tools are unavailable in the current environment.

## Final SRS Layout Polish
- Added the parent heading `2.9 DATA AND CLASS MODELS` before `2.9.1 ENTITY RELATIONSHIP DIAGRAM (ERD)` so the document no longer jumps directly from `2.8` to `2.9.1`.
- Standardized the heading `2.4.2 NON-FUNCTIONAL REQUIREMENTS`.
- Normalized curly punctuation in the document body to ASCII equivalents, including `86'd` and straight apostrophes.
- Formatted all `26` UC specification heading paragraphs so each UC begins on a new page and the heading stays with the following table.
- Compacted the `26` UC specification tables by applying `Table Grid`, fixed table/cell widths, 10pt table text, bold first-column labels, single line spacing, reduced cell margins, and `cantSplit` row behavior to reduce ugly page splitting.
- Scaled the tallest diagram image from approximately `8.81` inches high to `8.10` inches high so it fits more safely on an A4 page with the current margins.
- Verified with `python-docx`: outline now includes `2.4` and `2.9` parent headings, FR count remains `26`, each FR field count remains `26`, UC table count remains `26`, all UC headings have `page_break_before` and `keep_with_next`, all UC table rows have `cantSplit`, no corrupted `?` bullet remains, no stale `final overall Use Case Diagram` wording remains, and no curly apostrophe / en dash / em dash / non-breaking hyphen remains in the paragraph text.
- Created a safety backup at `tmp/docs/SRSReport_Class01_Group01.backup_pre_final_layout_fix.docx`; visual page-layout rendering remains unverified because `soffice`, `libreoffice`, and `pdftoppm` are unavailable in the current environment.
