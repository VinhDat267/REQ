# Journal - 2026-04-17

## Session Summary
- Locked the Wonton POS final business scope after the full-system baseline and P0 operational policies were accepted.
- Added explicit business scope lock language to root scope, business rules, README, AGENTS, and `.agents` guidance.
- Clarified that future final-project work should package, map, trace, translate, and refine the locked baseline rather than add new business capabilities.
- Added ADR `022-lock-final-business-scope.md`.

## Files Touched
- `All_Use_Cases.md`
- `Final_Project_Scope.md`
- `Business_Rules.md`
- `Business_Rules_EN.md`
- `README.md`
- `AGENTS.md`
- `.agents/context/current-status.md`
- `.agents/context/project-overview.md`
- `.agents/context/conventions.md`
- `.agents/specs/srs.md`
- `.agents/rules.md`
- `.agents/rules/02-scope-and-terms.md`
- `.agents/workflows/final_project_scope_check.md`
- `.agents/decisions/022-lock-final-business-scope.md`

## Outcome
- Business baseline is locked as of `2026-04-17`.
- Any future expansion requires explicit user approval and a scope-change ADR before final BRD / SRS / diagram work is updated.

## Use Case Inventory Alignment
- Updated `All_Use_Cases.md` with a Final Business Rule Coverage Addendum.
- Kept original `UC-01..UC-74` numbering unchanged.
- Mapped new locked business rules into existing use cases as operational policies, alternate flows, exception flows, audit rules, or reporting rules.
- Confirmed extension candidates such as split bill, merge table, offline order capture, loyalty, full accounting, native app, and Delivery remain outside the locked baseline.

## Review-Driven Refinement
- Strengthened the main `All_Use_Cases.md` table so final readers can still see real operational behavior without reading only the addendum.
- Clarified that the numbered inventory is `74` entries, but the active final baseline is `73` in-scope UCs plus the excluded `UC-50` Delivery placeholder.
- Surfaced shift close / reconciliation directly in `UC-41..UC-45`.
- Surfaced complaint / remake / wrong-handoff handling directly in `UC-51`, `UC-53`, `UC-68..UC-70`.
- Reframed audit logging as a cross-cutting control instead of treating `UC-74` as an audit catch-all.

## Granularity Refactor
- Added a canonical `CUC-xx` presentation layer to `All_Use_Cases.md` so the final use case diagram, BRD, and SRS can work with a moderate-sized use case set instead of exposing all `74` detailed entries at the same level.
- Kept the original `UC-01..UC-74` inventory unchanged as the detailed traceability layer.
- Marked small / supporting items such as promotion apply, notifications, invoice printing, and kitchen ticket printing as subflows or cross-cutting support rather than standalone canonical goals.
- Synced repo guidance so final-project documentation now consistently treats `CUC-xx` as the presentation layer and `UC-01..UC-74` as the inventory layer.

## Canonical Review Fixes
- Reduced the canonical layer from `26` to `25` CUCs after review.
- Merged the old kitchen-ticket-only canonical node into the main FOH order lifecycle instead of keeping a support flow as a standalone canonical use case.
- Merged the old micro service-closure canonical node into the FOH handoff lifecycle.
- Split table management and Pickup exception management into separate manager goals at canonical level.
- Added explicit canonical ownership tags (`primary` / `subflow` / `exception` / `support`) so each detailed `UC-xx` has one canonical owner.
- Added a canonical relationship layer (`include` / `extend` / `dependency`) to guide the final diagram and SRS consistently.

## Canonical Closure Pass
- Closed the remaining one-owner gap by assigning `UC-12` to `CUC-03` as a subflow and `UC-74` to `CUC-15` as support-only notification behavior.
- Clarified the canonical relationship convention so `extend` now reads in the correct business direction for prepayment vs order creation.
- Made mixed ownership rows explicit item-by-item where the old tag placement was ambiguous.
- Cleaned the final business-rule addendum so `UC-74` is no longer used as a quasi-catch-all reference for refund, outage, receipt, or pickup exception rules.

## Canonical Final Polish
- Narrowed the `CUC-15` title back to access/security only, while keeping `UC-74` as support-only ownership in the mapping.
- Atomized the canonical relationship layer so bundled target cells are now represented as one edge per row for diagram and SRS traceability.

## Final Traceability Sync
- Updated the use-case addendum so `Comp` and `Forfeited` are surfaced explicitly in pickup/takeaway no-show, complaint/remake, and reporting/reconciliation guidance.
- Expanded final-spec emphasis bullets so BRD / SRS writers see `Forfeited` and `Comp` directly in the use-case package instead of needing to infer them only from Business Rules.

## Canonical Full-System Diagram Source
- Added `docs/diagrams/plantuml/use-case/WontonPOS_FullSystem_Canonical_UseCase.puml` as the first editable final-project use case overview source beside the locked midterm diagram.
- The new diagram uses the canonical `CUC-01..CUC-25` layer, concrete actors only (`Registered Customer`, `Guest Customer`, `Manager`, `FOH Cashier`, `FOH Service`, `BOH Staff`), and keeps the main canvas limited to actor associations plus `include` / `extend`.
- Semantic `dependency` relations and the `UC-74` support-only rule remain documented in `All_Use_Cases.md` / legend form instead of being promoted into extra standalone UML nodes.
