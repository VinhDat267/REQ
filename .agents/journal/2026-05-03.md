# Journal - 2026-05-03

## Thesis Section III Client review fix

- Rewrote `Thesis.docx` section `III. Client: Overview, Problem Statement and Requirements` after a rubric-based review of the Client section.
- Improved rubric alignment by:
  - separating client overview from current information system description
  - collapsing the issue framing into one single core problem statement
  - replacing prose-only requirements with full-version FR/NFR tables
  - mapping the functional requirements to the curated `UC-01..UC-26` final use-case lane
  - adding representative detailed use case specifications for ordering, payment, menu management, kitchen intake, shift reconciliation, and operational exceptions
- Corrected scope / business-rule risks in the section:
  - Delivery remains out of scope and is not implied as an integration target
  - Bank Transfer QR is treated as cashier manual confirmation
  - inventory wording is limited to inventory-lite / 86'd availability handling
  - `order_status` and `payment_status` remain separate
- Created `Thesis_BACKUP_SectionIII_before_fix.docx` before editing the thesis file.
- Verification was completed with `python-docx`: Section III now includes one FR table (`27 x 5`), one NFR table (`10 x 4`), one UC conversion table (`27 x 5`), and six detailed UC specification tables (`11 x 2` each). Full DOCX visual rendering was not available because LibreOffice / Poppler commands were not present in the environment.

## Thesis final-brief polish pass

- Applied a follow-up thesis cleanup to `Thesis.docx` after reviewing it against `S2026_61FIT3REQ FINAL PROJECT.pdf`.
- Fixed final-brief alignment issues:
  - corrected `IV. Application of Elicitation Techniques`
  - fixed `Deliverables of of such techniques`
  - normalized Section V into a clean modelling outline: `1. Use Case Diagram`, `2. Class Diagram`, `3. Entity-Relationship Diagram`, `4. Details of Online Tools Applied`
  - corrected stale tool wording that mentioned DFD even though the thesis modelling set is Use Case / Class / ER
  - split `VIII. Revision of Project` out of the Prototype paragraph into a proper heading
  - added `Lesson Learned` and `Recommendation on Future BA Work`
  - revised third-party platform wording so Delivery/logistics integration remains a future-release candidate, not current baseline scope
  - standardized the references with accessed dates
- Created `Thesis_BACKUP_FinalBriefPolish_before_fix.docx` before editing.
- Verification via `python-docx` showed:
  - approximately `14,286` total words including table text
  - `18` references
  - `5` inline shapes retained
  - all text runs detected as Times New Roman `13`
  - no remaining `of of`, `GrabFood`, `ShopeeFood`, or stale DFD wording in document paragraphs
- Full page-render validation remains unavailable because LibreOffice / Poppler commands are not installed in the environment.

## Thesis Prototype section improvement

- Improved `Thesis.docx` section `VII. Prototype` after a rubric-based review of the Prototype requirement.
- Replaced the short prototype prose with a stronger UI/UX evidence section:
  - clarified that the `86` high-fidelity prototype screens exclude the catalog index page
  - separated `User Interface` and `User Experience` as real headings
  - added UI evidence for `35` Client WebApp screens and `51` Admin WebApp screens
  - added a prototype coverage table mapping screen groups to FR/NFR and `UC-01..UC-26`
  - expanded Client and Admin interface descriptions around checkout states, payment recovery, guest lookup, KDS, active orders, 86d availability, shift close, complaints, no-show, and outage recovery
  - added a `Prototype Validation Against Requirements` table
  - documented prototype limitations so the section does not overclaim production behavior
- Created `Thesis_BACKUP_Prototype_before_improve.docx` before editing.
- Verification via `python-docx` showed:
  - Section VII has `851` words
  - Section VII includes two tables: `4 x 4` and `7 x 3`
  - `User Experience` is now a real heading
  - the thesis has `20` tables, `5` inline shapes, and approximately `15,207` words including table text
- Full page-render validation remains unavailable because LibreOffice / Poppler commands are not installed in the environment.

## Thesis Prototype BA-language cleanup

- Cleaned `Thesis.docx` section `VII. Prototype` after review feedback that repo/internal terms were not appropriate for a Business Analysis final project.
- Reframed the section as a Figma-presented high-fidelity UI/UX prototype deliverable used for stakeholder walkthroughs, requirement validation, UI consistency review, and early journey/state validation.
- Removed the non-BA catalog/index row from the prototype coverage table, leaving only the `35` Client WebApp screens and `51` Admin WebApp screens as user-facing prototype evidence.
- Created `Thesis_BACKUP_Prototype_BA_language_before_fix.docx` before the cleanup.
- Verification via `python-docx` showed:
  - Section VII has `800` words
  - Section VII has zero `catalog`, `HTML`, `index page`, `screen-catalog`, or `import` strings
  - the prototype coverage table is now `3 x 4` with only `Client WebApp` and `Admin WebApp` rows
  - the thesis has `5` inline shapes and approximately `14,313` words including table text
- Full page-render validation remains unavailable because LibreOffice / Poppler commands are not installed in the environment.

## Thesis formal scope-language cleanup

- Cleaned remaining course/repo-style wording from `Thesis.docx`, especially Section `VII. Prototype`.
- Replaced non-formal phrasing such as `Software Requirements Analysis project`, `final-project operational baseline`, and `REQ final-project scope` with Business Analysis thesis wording:
  - `In this Business Analysis thesis`
  - `approved operational baseline`
  - `approved business scope`
- Also replaced body-level internal wording such as `project's visible final use-case lane`, `curated final use-case specification set`, and `final SRS package` with formal requirement-document wording.
- Changed `REQ ID` table headers to `Requirement ID`.
- Created `Thesis_BACKUP_FormalScopeLanguage_before_fix.docx` before editing.
- Verification via `python-docx` showed:
  - Section VII has zero `REQ`, `final-project`, `final project`, standalone `project`, `catalog`, `HTML`, `index page`, or `import` strings
  - full document has zero `final-project`, `REQ final-project scope`, `project's visible final`, or `REQ ID` strings
  - the thesis has `5` inline shapes and approximately `14,874` words including table text
- Full page-render validation remains unavailable because LibreOffice / Poppler commands are not installed in the environment.

## Thesis Section VIII revision improvement

- Improved `Thesis.docx` section `VIII. Revision of Project` after review feedback that the previous version read more like a short reflection than revision evidence.
- Replaced the section body with a stronger structure:
  - `Revision Summary`
  - an `8 x 5` revision table covering area reviewed, issue found, revision made, BA rationale, and resulting improvement
  - revised `Lesson Learned`
  - revised `Recommendation on Future BA Work`
- Kept the official outline heading while avoiding course/repo-style wording in the section body.
- Created `Thesis_BACKUP_SectionVIII_before_improve.docx` before editing.
- Verification via `python-docx` showed:
  - Section VIII appears before `References`
  - Section VIII has approximately `978` words including table text
  - the new revision table is `8 x 5`
  - Section VIII body has zero `REQ`, `final-project`, standalone `project`, `catalog`, `HTML`, `index page`, or `import` strings
  - the thesis has `21` tables, `5` inline shapes, and approximately `14,963` words including table text
- Full page-render validation remains unavailable because LibreOffice / Poppler commands are not installed in the environment.

## Thesis reference-citation hardening

- Reviewed `Thesis.docx` references and body citations after feedback that some claims need clearer source support.
- Confirmed the References section has `18` sources, while the body previously used only a smaller subset of them.
- Added in-text citations to Section `VI. Requirement Management` for requirement-management theory, traceability, change control, prioritization, effort assessment, and requirement-risk handling.
- Newly connected body citations to sources already listed in References:
  - IBM for requirements management, traceability, change management, revision, and document updates
  - Techcanvass for requirement prioritization
  - Business Analyst Learnings for requirement-risk identification, probability/impact ranking, owners, and monitoring
  - ModernAnalyst for requirement-risk impact and change-control considerations
  - Kassab and Destefanis plus Rak / Car / Lovrek for effort-estimation reasoning
- Created `Thesis_BACKUP_ReferenceCitation_before_fix.docx` before editing.
- Verification via `python-docx` showed:
  - `18` references
  - `21` detected body citations
  - all newly cited sources are present in References
  - the thesis has `21` tables, `5` inline shapes, and approximately `15,191` words including table text
- Full page-render validation remains unavailable because LibreOffice / Poppler commands are not installed in the environment.
