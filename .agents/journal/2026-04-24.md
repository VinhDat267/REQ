# 2026-04-24

## Frontend Final-Business Flow Expansion
- Extended the static HTML prototype suite under `src/frontend/` from the previous midterm-era coverage into a broader final-project import catalog.
- Added `4` Client WebApp screens:
  - `client/11-reorder-menu-drift.html`
  - `client/12-customer-account-profile.html`
  - `client/13-guest-order-lookup-error.html`
  - `client/14-payment-recovery-refund-status.html`
- Added `8` Admin WebApp screens:
  - `admin/16c-promotion-usage-audit.html`
  - `admin/19-active-orders-command-center.html`
  - `admin/20-shift-close-reconciliation.html`
  - `admin/21-menu-availability-86d.html`
  - `admin/22-serve-handoff-queue.html`
  - `admin/23-complaint-exception-resolution.html`
  - `admin/24-pickup-no-show-exception.html`
  - `admin/25-outage-recovery-workbench.html`
- Updated `src/frontend/index.html` into a complete catalog of `56` screens (`24` Client + `32` Admin) for Figma-oriented import and review.
- Updated `src/frontend/shared/nav-components.js` so the new Admin screens resolve to their correct sidebar/header groups and the new Client account screen appears in the customer account menu.
- Connected existing prototype paths:
  - order-history reorder action now routes to the reorder/menu-drift screen
  - KDS 86'd action now routes to the availability/86'd control screen

## Business Alignment Notes
- No new business capabilities were introduced.
- The new screens expose already-locked final baseline behavior: separate `order_status` / `payment_status`, Takeaway/Pickup prepayment, QR/manual payment recovery, late callback refund handling, shift close reconciliation buckets, `Comp`, `Forfeited`, `Write-off`, 86'd controls, wrong-handoff correction, complaint/remake/refund decisions, and manual outage recovery.
- `Delivery` remains out of scope.

## Verification
- `git diff --check` completed with only line-ending warnings.
- Confirmed `src/frontend/index.html` links resolve.
- Confirmed all relative HTML links inside HTML files resolve.
- Confirmed the prototype suite now contains `56` HTML screen files.

## Frontend Consistency Fix Follow-up
- Fixed the prototype catalog after the user added more frontend states; `src/frontend/index.html` now reflects `67` screens (`35` Client + `32` Admin) and links all Client state screens added after the first pass.
- Extended `src/frontend/shared/nav-components.js` mapping for checkout, tracking, history, guest lookup, and Admin subpage variants so state pages no longer fall back to the Dashboard/Home active state.
- Standardized the Guest lookup screens to use a guest-only shell instead of mixing shared logged-in navigation with a no-user-menu view.
- Replaced out-of-scope `Thẻ / POS` wording with locked-scope payment wording: cash, manual bank-transfer QR, online gateway, refund/write-off buckets.
- Removed trailing whitespace in the revenue report screen so `git diff --check` can pass cleanly apart from line-ending warnings.

## Frontend Prototype Link Pass
- Converted existing action CTAs into static HTML anchors on prototype screens that previously depended only on dynamic shared navigation or had no outgoing link.
- Connected Admin flows across menu category/topping, revenue report, promotion usage audit, Pickup schedule, active orders, shift close, 86'd availability, handoff, complaint resolution, Pickup no-show, and outage recovery screens.
- Connected the Guest lookup rate-limit state back into the Guest lookup success/error support path.
- Verified the frontend HTML graph has no orphan screens, no terminal-only screen without a related outgoing link, and no broken relative `href/src` target.

## Frontend Price Consistency Pass
- Normalized repeated menu item prices across Client/Admin prototype screens:
  - Mì vằn thắn nước / tôm thịt: `65,000`
  - Mì khô / xá xíu: `60,000`
  - Sủi cảo Tứ Xuyên: `75,000`
  - Trà sữa / trà lài: `29,000`
  - Hoành thánh thịt heo: `55,000`
  - Hoành thánh chiên: `35,000`
  - Há cảo hải sản: `96,000`
  - Đóng gói: `5,000`
- Recalculated order totals where the line items are visible: Admin order/cancel/payment, Admin assign order, Client menu/detail/cart, cancelled tracking, reorder drift, and revenue top-item rows.
- Kept aggregate KPI/report totals and standalone payment/refund totals unchanged where no item breakdown is shown.

## Frontend Responsive Hardening
- Fixed mobile/tablet responsive risks in the highest-risk HTML prototype screens found by the Chrome render audit:
  - Admin dashboard / notification recent-order tables now use internal horizontal scroll instead of page overflow.
  - Admin order management, cancel modal, assign-table, payment, report, category/topping, active-order, and 86'd screens now collapse split desktop layouts and fixed-width controls for small screens.
  - Client order-history detail / empty / cancelled screens now stack list/detail panes on mobile and use scrollable tab rows.
  - Shared Admin shell now constrains page-level horizontal overflow and compresses the header actions on small screens.
- Re-ran a Chrome render audit over `68` HTML files at mobile `390x844`, tablet `768x1024`, and desktop `1440x900`; result: `0` page-level horizontal-scroll screens and `0` severe clipped-text screens.
- Re-ran regression checks for the earlier frontend review findings: no mojibake candidates, catalog remains `67` screens (`35` Client + `32` Admin), no missing catalog links, required nav map entries are present, guest lookup screens remain guest-only, and no broken relative HTML links were detected.
