# 2026-04-07

## Summary
- Audited `src/frontend-vi/` against `src/frontend/`, `BRD_Ver0.md`, and `Business_Rules.md`
- Confirmed the Vietnamese frontend is not yet 100% Vietnamese and not yet semantically clean
- Patched a targeted subset of leftover English strings and business-term mismatches
- Discovered a broader mojibake/diacritic corruption issue across the entire `frontend-vi` tree

## Key Findings
- `Pickup` vs `Takeaway` terminology had drifted in several client screens and needed BRD-aligned wording
- Visible English copy still remained in both client and admin screens after the initial translation pass
- All 25 HTML files under `src/frontend-vi/` still contain broken Vietnamese strings (`?`, mojibake, or damaged diacritics), so the artifact cannot yet be certified as a clean Vietnamese deliverable

## Follow-up
- Run a dedicated screen-by-screen normalization pass for `src/frontend-vi/`
- Re-check titles, placeholders, alt text, footer copy, timestamps, and summary labels after diacritic restoration
- Re-run the English-leftover and corrupted-string scans before treating the Vietnamese frontend as complete

## Update - Normalization Pass Completed
- Built `scripts/normalize_frontend_vi.py` to regenerate Vietnamese HTML from the clean English source using a UTF-8-safe workflow
- Added BRD-aligned glossary overrides for key service terms such as `Ăn tại quán`, `Mang đi`, and `Hẹn giờ đến lấy`
- Regenerated all 25 HTML files under `src/frontend-vi/` in smaller batches after a full-run timeout, then applied a fast post-normalization cleanup for brand/name consistency
- Rewrote `src/frontend-vi/shared/nav-components.js` so the runtime-injected navigation no longer reintroduces mojibake
- Verified with automated UTF-8 scans that the Vietnamese HTML screens no longer expose visible English UI terms or visible questionable `??` text

## Residual Risk
- The current pass is structurally clean and terminology-aligned, but some long descriptive copy may still need human polish for more natural Vietnamese phrasing before final submission/review

## Update - Manual Polish Pass Completed
- Re-ran an adversarial audit over `src/frontend-vi/` and treated the previous "clean" status as provisional until checked against visible DOM text and human-readable phrasing
- Fixed the highest-risk visible copy issues in `index.html`, client checkout/tracking screens, and the main admin overview/notification screens
- Extended the cleanup to the remaining admin screens after verification exposed repeated machine-translated labels such as `xưởng may`, `Xưởng chính xác`, `nấu ăn`, and `Gán thứ tự vào bảng`
- Standardized visible admin branding to `Wonton POS`, improved several notification/order-status phrases, and normalized reviewed time labels to remove stray `PM` formatting from the Vietnamese UI
- Re-verified with a visible-text scan that the reviewed `frontend-vi` HTML no longer exposes the targeted visible English/UI leftovers; remaining raw-string hits are limited to HTML comments
- Cleaned the remaining noisy HTML comments tied to the same old markers so the targeted raw-string scan is now clean as well
