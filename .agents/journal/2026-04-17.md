# Journal - 2026-04-17

## Session Summary
- Locked the Wonton POS final business scope after the full-system baseline and P0 operational policies were accepted.
- Added explicit business scope lock language to root scope, business rules, README, AGENTS, and `.agents` guidance.
- Clarified that future final-project work should package, map, trace, translate, and refine the locked baseline rather than add new business capabilities.
- Added ADR `022-lock-final-business-scope.md`.

## Files Touched
- `All_Use_Cases.md`
- `Final_Project_Scope.md`
- `Business_Rules.md`
- `Business_Rules_EN.md`
- `README.md`
- `AGENTS.md`
- `.agents/context/current-status.md`
- `.agents/context/project-overview.md`
- `.agents/context/conventions.md`
- `.agents/specs/srs.md`
- `.agents/rules.md`
- `.agents/rules/02-scope-and-terms.md`
- `.agents/workflows/final_project_scope_check.md`
- `.agents/decisions/022-lock-final-business-scope.md`

## Outcome
- Business baseline is locked as of `2026-04-17`.
- Any future expansion requires explicit user approval and a scope-change ADR before final BRD / SRS / diagram work is updated.

## Use Case Inventory Alignment
- Updated `All_Use_Cases.md` with a Final Business Rule Coverage Addendum.
- Kept original `UC-01..UC-74` numbering unchanged.
- Mapped new locked business rules into existing use cases as operational policies, alternate flows, exception flows, audit rules, or reporting rules.
- Confirmed extension candidates such as split bill, merge table, offline order capture, loyalty, full accounting, native app, and Delivery remain outside the locked baseline.

## Review-Driven Refinement
- Strengthened the main `All_Use_Cases.md` table so final readers can still see real operational behavior without reading only the addendum.
- Clarified that the numbered inventory is `74` entries, but the active final baseline is `73` in-scope UCs plus the excluded `UC-50` Delivery placeholder.
- Surfaced shift close / reconciliation directly in `UC-41..UC-45`.
- Surfaced complaint / remake / wrong-handoff handling directly in `UC-51`, `UC-53`, `UC-68..UC-70`.
- Reframed audit logging as a cross-cutting control instead of treating `UC-74` as an audit catch-all.

## Final Traceability Sync
- Updated the use-case addendum so `Comp` and `Forfeited` are surfaced explicitly in pickup/takeaway no-show, complaint/remake, and reporting/reconciliation guidance.
- Expanded final-spec emphasis bullets so BRD / SRS writers see `Forfeited` and `Comp` directly in the use-case package instead of needing to infer them only from Business Rules.

## Use Case Inventory Cleanup
- Restored the final-project use-case package to a single-layer `UC-01..UC-74` structure.
- Aligned final BRD / SRS / UC specifications directly to the current use-case document set.
- Kept `All_Use_Cases.md` as the single whole-system numbering source for final-project work.

## Final-Project Default Clarification
- Reaffirmed that the repository is no longer a midterm-scoped workspace.
- Tightened bootstrap, rules, and scope docs so current work defaults to final-project whole-system artifacts.
- Kept midterm materials in the repo only as archival submission / traceability references.

## BRD Final V3 Numbering Adjustment
- Approved a curated local authoring lane for `BRD final v3` so the team can keep the current visible `UC-01..UC-16` IDs and extend them with selected `UC-17+`.
- Kept `All_Use_Cases.md` as the full-system master inventory / scope-check source instead of using it as the forced visible numbering lane for the BRD narrative.
- Added `BRD_Final_V3_UC_Shortlist.md` to define the local final BRD set, initially at `24` UCs before the later same-day expansion to `26`.
- Added ADR `030-allow-curated-local-final-brd-uc-set.md`.
- Synced README, scope docs, rules, context files, and UC specification headers so the repo no longer pushes the BRD back to the original master IDs by default.

## Final BRD Collaboration Rule
- Clarified that the old midterm `8 members x 2 UCs` split does not carry over to `BRD final v3`.
- Final BRD v3 is now treated as one shared team-authored document rather than a set of member-owned BRD slices.
- Added ADR `031-final-brd-v3-is-shared-team-document.md`.
- Updated the BRD final shortlist, scope/status docs, and UC specification headers so the repo treats the old assignment tables as historical context only.

## Final BRD Shortlist Expansion
- Re-reviewed the local final BRD set and kept all existing additions.
- Expanded the recommended shortlist from `24` to `26` UCs by adding two customer-facing gaps that were still underrepresented:
  - `UC-25` Quan ly tai khoan khach hang va xac thuc
  - `UC-26` Hoan tat checkout va ap dung uu dai
- Added ADR `032-expand-final-brd-shortlist-to-26-ucs.md`.
- Updated the shortlist so the final BRD lane now recommends `UC-01..UC-26`.

## Final BRD UC Catalog Standardization
- Standardized the local final BRD set into a ready-to-paste `26`-UC catalog inside `BRD_Final_V3_UC_Shortlist.md`.
- Added BRD-facing UC titles, one-line descriptions, representative coverage clusters, and a coverage-check section explaining why the `26` UCs are enough to communicate the full system at BRD level.

## BRD Ver3 DOCX Sync
- Resynced `Group1_Tutorial01_BRD_Ver3.docx` against `UC_Specifications_Part1_EN.md` and `UC_Specifications_Part2_EN.md`.
- Updated the cover-page date to `2026-4-17`.
- Aligned the BRD functional-requirement wording with the current UC set.
- Corrected the previously missed DOCX UC tables `UC-08`, `UC-09`, `UC-10`, and `UC-12`, which had been skipped by the earlier sync because the Word file used `UC 08/09/10/12` formatting instead of `UC-08/09/10/12`.
- Removed the duplicate intermediate version-history row introduced during the first partial sync attempt.
- Added page breaks before each UC specification table from `UC-02..UC-26` so each UC now starts on a separate page instead of later tables running together continuously.
- Verified the updated DOCX content through `python-docx` table/text inspection; full visual page-layout validation remains pending because LibreOffice/Poppler rendering tools are not available in the current environment.

## BRD Ver3 UC-17+ Review Fix
- Applied direct corrections to the `UC-17..UC-26` block in `Group1_Tutorial01_BRD_Ver3.docx` after a review against the locked final baseline.
- Fixed seven issues:
  - added valid special-note carry-over to `UC-17 Reorder`
  - expanded `UC-18 Manage Promotions` to include usage-recovery and failed-application audit coverage
  - expanded `UC-20 Manage Active Orders` so it covers actionable exceptions and `Completed + Unpaid` Dine-in follow-up
  - expanded `UC-21 Close Shift and Reconcile End-of-day` so the review set includes aged pending payments, overdue ready orders, wrong-handoff / duplicate-charge related cases, and inherited-exception handling
  - replaced the future-leaning bulk-86'd alternate flow in `UC-22`
  - corrected the inconsistent alternative flow in `UC-23 Serve and Confirm Handoff`
  - normalized `UC-25 Manage Customer Account and Authentication` so actor, trigger, and preconditions match the included flows
- Removed repo-internal and non-formal wording from the BRD itself, including phrases such as `BRD final v3`, `final-project`, `authoring lane`, and ADR-tag references in rationale cells.
- Rewrote affected Version History / Assumption / FR / NFR cells in a formal BRD tone.

## BRD Ver3 Project Overview Fix
- Reworked the `Project Overview` section in `Group1_Tutorial01_BRD_Ver3.docx` into a formal four-part structure:
  - `4.1.1 Selected System`
  - `4.1.2 Project Description`
  - `4.1.3 Problem Statement`
  - `4.1.4 Project Statement`
- Replaced the earlier multi-bullet problem framing with one core problem statement in the main BRD body.
- Updated the business-case appendix so it now presents one core business problem followed by business impacts instead of multiple separate problems.
- Expanded the rewritten `Project Statement` so it now states the delivered system components and explains the value for customers, shop staff, developers, testers, and the board of management.
- Verified by `python-docx` text inspection that the BRD now contains a single explicit `Problem Statement` heading in the main overview section; visual page-layout validation is still pending because LibreOffice/Poppler tooling are unavailable in the current environment.

## BRD Ver3 Stakeholder Interview Coverage Fix
- Updated `Group1_Tutorial01_BRD_Ver3.docx` stakeholder tables so the stakeholder/interview coverage now includes:
  - Nguyen Quang Anh as project sponsor / CEO
  - FOH Manager Representative for cashier, server, table, counter-ordering, payment coordination, handoff, and service recovery requirements
  - BOH Manager Representative for kitchen workflow, KDS visibility, item availability, 86'd handling, preparation status, and fulfilment exception requirements
- Updated the elicitation DOCX set so preparation, interview, questionnaire, and meeting notes no longer imply that only Nguyen Quang Anh was interviewed:
  - `docs/requirement-elicitation/01_Prepare_for_Elicitation_Wonton_POS.docx`
  - `docs/requirement-elicitation/02_Interview_Elicitation_Notes_Wonton_POS.docx`
  - `docs/requirement-elicitation/03_Questionnaire_Elicitation_Notes_Wonton_POS.docx`
  - `docs/requirement-elicitation/04_Meeting_Elicitation_Notes_Wonton_POS.docx`
- Used role-based placeholders (`FOH Manager Representative`, `BOH Manager Representative`) because no formal personal names for those managers are present in the source documents yet.
- Verified the updated DOCX content at text/table level. Visual rendering is still pending because LibreOffice/Poppler tools are unavailable in the current environment.

## 26-UC Markdown Specification Export
- Created `UC_Specifications_26_EN.md` as a standalone Markdown file containing the full `UC-01..UC-26` specification set from `Group1_Tutorial01_BRD_Ver3.docx`.
- Preserved the current BRD Ver 3 UC wording, including the reviewed `UC-17..UC-26` corrections.
- Verified the export contains exactly `26` UC sections, from `UC-01: Place Online Order` through `UC-26: Complete Checkout and Apply Promotion`.

## PlantUML UC-25 Activity Diagram With Swimlanes
- Created `docs/diagrams/plantuml/activity/UC-25_ManageCustomerAccountAndAuthentication.puml` using the PlantUML activity diagram new syntax guide in `docs/reference/PlantUML_Activity_Diagram_New_Syntax_Guide.md`.
- Scoped the diagram specifically to `UC-25: Manage Customer Account and Authentication`.
- Modeled the account lifecycle across only two swimlanes: `Customer` and `System`.
- Included branches for log in, register, forgot password, update profile, change password, and log out.
- Removed the earlier broad order-lifecycle diagram file so the delivered activity diagram matches the requested UC-25-only scope.
- Revised the diagram to remove mid-branch `stop` statements that caused dangling / empty connectors in PlantUML rendering; all branches now merge into one final account-state step before the single terminal node.
- Refactored the diagram again to remove the wide `switch/case` branch layout that produced long crossing connector lines in rendered images. The diagram now uses a cleaner vertical validation flow with compact notes for action-specific examples.
- Removed custom colors from the UC-25 PlantUML source so the rendered activity diagram uses a black-and-white/default PlantUML style.
- Performed static structure checks for balanced `@startuml/@enduml`, `if/endif`, a single terminal `stop`, and exactly two swimlane declarations (`Customer`, `System`). PNG/SVG rendering was not executed because the local `plantuml` command is unavailable.

## Activity Diagram Authoring Rule
- Standardized the UC-25 activity diagram style as the default rule for future PlantUML activity diagrams.
- Required black-and-white styling for node fills, borders, diamonds, connectors, arrowheads, notes, swimlane borders, and text.
- Required business-level swimlanes instead of internal technical layers:
  - customer-facing flows use `Customer` and `System`
  - `Payment Gateway` is added only when the flow explicitly interacts with an external payment provider or callback
  - internal details such as web app, database, authentication service, KDS, printer, and notification handling stay inside `System`
- Added ADR `033-standardize-activity-diagram-style.md`.

## Canonical Activity Diagram Guide
- Adopted root `activity-diagram-guide.md` as the canonical guide for Activity Diagram work.
- Updated `AGENTS.md`, `README.md`, `.agents/rules.md`, `.agents/rules/04-diagram-policy.md`, `.agents/context/conventions.md`, `.agents/context/codex-bridge.md`, and `.agents/README.md` so future agents know to apply the guide before drawing or reviewing ADs.
- Clarified that the guide governs UML semantics and quality checks, while the UC-25 convention governs Wonton POS presentation style.
- Added ADR `034-adopt-activity-diagram-guide.md`.

## UC-25 Activity Diagram Guide Compliance Fix
- Refactored `docs/diagrams/plantuml/activity/UC-25_ManageCustomerAccountAndAuthentication.puml` after reviewing it against `activity-diagram-guide.md`.
- Replaced the earlier generic validation path with explicit account-action branches:
  - log in
  - register
  - forgot password
  - update profile
  - change password
  - log out
- Changed exception branches for invalid credentials, invalid profile/registration data, invalid OTP, failed password policy, and missing authentication so they no longer flow into the success path.
- Kept the project presentation convention intact: black-and-white styling, business-level swimlanes, and only `Customer` / `System` lanes.
- Static checks passed for balanced `@startuml/@enduml`, `start/stop`, `if/endif`, no hex colors, and no forbidden technical swimlanes. PNG/SVG rendering remains pending because the local `plantuml` command is unavailable.

## UC-25 Activity Diagram Connector Fix
- Replaced every `detach` in `docs/diagrams/plantuml/activity/UC-25_ManageCustomerAccountAndAuthentication.puml` with an explicit `stop`.
- Root cause: `detach` prevented exception branches from falling into the success path, but PlantUML renders those exits as dangling-looking connectors / blank branch endings.
- Refactored the diagram away from a single large merged `if/elseif` block into a vertical decision chain so each UC-25 account action reaches an explicit success or exception final node.
- Rendered the updated diagram with `tmp-plantuml-1.2026.2.jar` to PNG and SVG under `docs/diagrams/plantuml/activity/rendered/`; the rendered PNG is `1325 x 3773`, uses only black-and-white styling, and no longer relies on blank / detached branch endings.

## UC-25 UML Terminal Node Fix
- Added a stricter Activity Diagram style rule for UML terminal nodes:
  - `start` must render as a solid black circle
  - final nodes must render as a white outer circle with black border and black inner circle
- Root cause: the UC-25 PlantUML source contained a `<style>` block that overrode `activityStartColor` / `activityEndColor`, which made both the start node and final nodes render white.
- Fixed `docs/diagrams/plantuml/activity/UC-25_ManageCustomerAccountAndAuthentication.puml` by removing the conflicting `<style>` block and keeping explicit black/white `skinparam` settings instead.
- Added ADR `035-enforce-uml-activity-terminal-node-style.md` and synced the rule into `.agents/rules.md`, `.agents/rules/04-diagram-policy.md`, and `.agents/context/conventions.md`.
- Synced the same UML terminal-node rule into the root `activity-diagram-guide.md` so the canonical AD guide now explicitly states the required rendering for Initial Node and Activity Final Node.
- Re-rendered the UC-25 diagram to PNG/SVG and verified that the start node is black and the final nodes use the UML double-circle final-state style.

## Per-Use-Case Process Flow Diagram Standard
- Standardized the expectation that each selected use case should maintain both:
  - one UML Activity Diagram (`AD`) for formal use-case / system behavior modeling
  - one Process Flow Diagram (`PFD`) for stakeholder-friendly business workflow communication
- Promoted `process-flow-diagram-guide.md` as the canonical guide for PFD creation and review.
- Promoted `process-vs-activity-decision-guide.md` as the canonical decision guide for separating PFD vs AD notation and avoiding accidental hybrid diagrams.
- Synced the rule into `AGENTS.md`, `README.md`, `.agents/rules.md`, `.agents/rules/04-diagram-policy.md`, `.agents/context/conventions.md`, `.agents/context/architecture.md`, `.agents/context/codex-bridge.md`, and `.agents/README.md`.
- Added ADR `036-standardize-per-use-case-process-flow-diagrams.md`.

## UC-25 Process Flow Diagram
- Created `docs/diagrams/plantuml/process-flow/UC-25_ManageCustomerAccountAndAuthentication_PFD.puml`.
- Used PlantUML as requested, with no swimlanes.
- Reused the UC-25 account-action logic from the Activity Diagram but kept the artifact as a Process Flow Diagram focused on readable business workflow.
- Included branches for update profile, change password, log out, log in, register, forgot password, and unsupported account action.
- Rendered PNG/SVG outputs under `docs/diagrams/plantuml/process-flow/rendered/`.
- Verified the source with PlantUML `-checkonly`; static checks showed `0` swimlanes, balanced `if/endif`, and no `detach`.

## PlantUML-Only Diagram Rule
- Tightened the diagram policy so all new or revised canonical diagram sources must use PlantUML.
- Marked Mermaid as disallowed for new diagram sources; existing Mermaid files are legacy references until intentionally converted.
- Tightened the PFD rule so Process Flow Diagrams must not use swimlanes.
- Updated `process-flow-diagram-guide.md` and `process-vs-activity-decision-guide.md` with the project-specific PlantUML-only / PFD-no-swimlane policy.
- Synced the rule into `AGENTS.md`, `README.md`, `.agents/rules.md`, `.agents/rules/04-diagram-policy.md`, `.agents/context/conventions.md`, `.agents/context/architecture.md`, and `.agents/context/current-status.md`.
- Added ADR `037-require-plantuml-diagram-sources.md`.

## UC-17 Activity Diagram
- Created `docs/diagrams/plantuml/activity/UC-17_ReorderPastOrder.puml`.
- Followed the UC-25 Activity Diagram style: PlantUML new syntax, black-and-white rendering, business-level swimlanes, UML-compliant start/final nodes, and no technical lanes.
- Modeled `Registered Customer` and `System` responsibilities for reordering a completed past order.
- Covered source-order eligibility, Reorder feature availability, all-items-unavailable failure, menu drift, dropped items/options, current pricing, valid note carry-over, 86'd checkout blockers, invalid old promotion handling, and routing to checkout `UC-26`.
- Rendered PNG/SVG outputs under `docs/diagrams/plantuml/activity/rendered/`.
- Verified the source with PlantUML `-checkonly`; static checks showed balanced `if/endif`, no `detach`, and no Mermaid syntax.

## UC-17 Process Flow Diagram
- Created `docs/diagrams/plantuml/process-flow/UC-17_ReorderPastOrder_PFD.puml`.
- Followed the UC-25 PFD style: PlantUML only, no swimlanes, business-level process wording, and responsibility shown directly in step labels.
- Covered source-order eligibility, Reorder feature toggle, all-items-unavailable stop path, menu drift handling, current repricing, valid note carry-forward, 86'd checkout blockers, old-promotion notice handling, and routing to checkout `UC-26`.
- Rendered PNG/SVG outputs under `docs/diagrams/plantuml/process-flow/rendered/`.
- Verified the source with PlantUML `-checkonly`; static checks showed balanced `if/endif`, no `detach`, no Mermaid syntax, and `0` swimlanes.

## Compact Re-render Pass For UC-17 And UC-25
- Applied a compact-layout pass to the `UC-17` Activity Diagram and Process Flow Diagram by removing forced vertical-if layout and tightening repeated labels so the diagrams lean slightly more horizontal without becoming overly wide.
- Applied the same compact-layout pass to the `UC-25` Activity Diagram and Process Flow Diagram, then re-rendered both PNG and SVG outputs into the existing `rendered` folders.
- Verified the updated `UC-25` sources with PlantUML `-checkonly` before rendering.
- Post-pass render measurements:
  - `UC-25` AD: from `1325 x 3773` to `1322 x 3625`
  - `UC-25` PFD: re-rendered successfully; overall height remained near the previous value because the branch structure is still mostly sequential

## UC-18 Activity Diagram And Process Flow Diagram
- Created `docs/diagrams/plantuml/activity/UC-18_ManagePromotions.puml`.
- Created `docs/diagrams/plantuml/process-flow/UC-18_ManagePromotions_PFD.puml`.
- Modeled the `Manager` / `System` promotion-management flow around the selected action branches:
  - create new promotion
  - edit promotion
  - deactivate promotion
  - view usage history
  - review failed application attempts
- Kept the AD in the project's black-and-white UML style with business-level swimlanes only.
- Kept the PFD PlantUML-only and lane-free, with responsibility shown directly in step labels.
- Rendered PNG/SVG outputs under both `docs/diagrams/plantuml/activity/rendered/` and `docs/diagrams/plantuml/process-flow/rendered/`.
- Verified both sources with PlantUML `-checkonly`; static checks showed:
  - AD: balanced `if/endif`, no `detach`
  - PFD: balanced `if/endif`, no `detach`, no Mermaid syntax, and `0` swimlanes

## PFD Terminal And Decision Notation Update
- Updated the repository-wide PFD rule so:
  - `Start` / `End` use white ellipse terminals
  - decision points use white diamond nodes
- Added ADR `038-standardize-pfd-ellipse-terminals-and-diamond-decisions.md`.
- Updated `AGENTS.md`, `README.md`, `.agents/rules.md`, `.agents/rules/04-diagram-policy.md`, `.agents/context/conventions.md`, `process-flow-diagram-guide.md`, and `process-vs-activity-decision-guide.md` to carry the same notation rule.
- Rebuilt the current PFD sources (`UC-17`, `UC-18`, `UC-25`) using PlantUML `allowmixing` notation so rendered PNG/SVG outputs now match the new convention.
- In the current PlantUML pattern:
  - terminals are rendered with `usecase` ellipse nodes
  - process steps are rendered with `rectangle` nodes
  - decision points are rendered with `diamond` nodes
  - decision questions may be placed on the incoming connector when keeping the diamond visually clean yields a more readable result

## PFD Visual Cleanup Pass
- Applied a follow-up visual cleanup after review feedback on the rendered `UC-17` PFD.
- Root cause: the mixed-element PlantUML render was leaving decision diamonds with colored styling that read as yellow/red instead of the intended white-background / black-border notation.
- Fixed by adding explicit PlantUML `<style>` blocks for `diamond`, `rectangle`, `usecase`, and `note`, and by forcing `BackgroundColor White` directly in the PFD sources.
- Re-rendered `UC-17`, `UC-18`, and `UC-25` PFD PNG/SVG outputs so the current process-flow set uses consistent black-and-white terminal / decision styling.

## PFD Render Output Path Fix
- Investigated the follow-up review report that `UC-17` still looked like it used Activity Diagram terminal nodes even though the source had already been converted to white ellipse PFD terminals.
- Root cause: the PFD sources were correct, but one PlantUML rerender pass wrote fresh assets into an accidental nested folder under `docs/diagrams/plantuml/process-flow/docs/diagrams/plantuml/process-flow/rendered/`, while the intended `docs/diagrams/plantuml/process-flow/rendered/` folder still contained older stale images.
- Re-rendered `UC-17`, `UC-18`, and `UC-25` directly into the intended `rendered/` folder using `tmp-plantuml-1.2026.2.jar`.
- Verified from the refreshed `UC-17` SVG/PNG output that:
  - `Start` is rendered as a white ellipse terminal
  - `End` nodes are rendered as white ellipse terminals
  - decision nodes remain white diamonds with black borders
- Removed the accidental nested output folder and temporary local PlantUML test files used during diagnosis.

## PFD Layout Cleanup Pass
- Followed up on review feedback that the refreshed PFD set still had too many overlapping / crossing connector lines, especially in `UC-18` and `UC-25`.
- Kept the earlier branch-spread cleanup already applied to `UC-17`; the additional work focused on the more tangled promotion and account-management flows.
- Refactored `docs/diagrams/plantuml/process-flow/UC-18_ManagePromotions_PFD.puml` into a clearer selector chain with local branch endings for:
  - create new promotion
  - edit promotion
  - deactivate promotion
  - view usage history
  - review failed attempts
- Refactored `docs/diagrams/plantuml/process-flow/UC-25_ManageCustomerAccountAndAuthentication_PFD.puml` more aggressively by separating the menu into two larger groups:
  - signed-in actions (`Update profile`, `Change password`, `Log out`)
  - public actions (`Log in`, `Register`, `Forgot password`)
- Switched the two hardest files (`UC-18`, `UC-25`) to orthogonal routing after the branch restructuring so the remaining connectors read as cleaner right-angle paths instead of long diagonals.
- Re-rendered the canonical PFD outputs under `docs/diagrams/plantuml/process-flow/rendered/` and verified the updated timestamps:
  - `UC-17`: `2026-04-18 00:19`
  - `UC-18`: `2026-04-18 00:19`
  - `UC-25`: `2026-04-18 00:21`
- Re-ran PlantUML `-checkonly` successfully on the updated sources before the final rerender.

## Mermaid PFD Canonical Format Change
- Accepted the user's decision that Process Flow Diagrams are closer to flowcharts and should use Mermaid instead of PlantUML.
- Added ADR `039-use-mermaid-for-process-flow-diagrams.md`.
- Updated the rule layer so:
  - UML Activity Diagrams, use-case diagrams, ERDs, and other UML-style diagrams remain PlantUML-first
  - Process Flow Diagrams are now Mermaid `flowchart` artifacts
  - PFD sources belong under `docs/diagrams/mermaid/process-flow/`
  - PFD rendered assets belong under `docs/diagrams/mermaid/process-flow/rendered/`
- Created Mermaid PFD sources and rendered SVG/PNG outputs for:
  - `UC-17_ReorderPastOrder_PFD`
  - `UC-18_ManagePromotions_PFD`
  - `UC-25_ManageCustomerAccountAndAuthentication_PFD`
- Rendered the Mermaid PFDs with Mermaid CLI `11.12.0` via `npx @mermaid-js/mermaid-cli`.
