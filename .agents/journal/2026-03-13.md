# Journal - 2026-03-13

## Những gì đã làm
- Mở rộng `scripts/relayout_activity_drawio.py` từ relayout thuần sang workflow controlled auto-fix cho activity draw.io.
- Đổi data model fix sang dạng explicit theo `file -> cell id -> field -> { old, new }`.
- Mở rộng field allowlist cho auto-fix: `value`, `x`, `parent`, `source`, `target`.
- Thêm typed validation theo loại cell:
  - `parent` chỉ áp dụng cho non-swimlane vertex
  - `source` / `target` chỉ áp dụng cho edge
  - `x` yêu cầu có `mxGeometry`
- Mở rộng snapshot/invariant checking để track thêm `parent`, `source`, `target`.
- Cập nhật invariant logic để chỉ cho phép structural drift nếu nằm trong allowlist explicit của fix map.
- Đổi execution order để apply controlled fixes trước các bước graph-dependent như routing và guard checks.
- Mở rộng summary output với các trường audit:
  - `targeted_changed_fields`
  - `parent_moves`
  - `rewired_edges`
- Áp dụng scoped fix cho 3 file ưu tiên cao:
  - `docs/diagrams/drawio/activity/UC-13_ReceiveKitchenOrders.drawio`
  - `docs/diagrams/drawio/activity/UC-15_RateOrder.drawio`
  - `docs/diagrams/drawio/activity/UC-16_ReceiveOrderNotifications.drawio`
- Đồng bộ project brain (`.agents/`) và project memory để phản ánh capability mới của script canonical.

## Kết quả
- `scripts/relayout_activity_drawio.py` hiện là script canonical cho cả:
  - batch relayout hình học
  - deterministic spec-alignment auto-fix có kiểm soát
- Scoped run cho `UC-13`, `UC-15`, `UC-16` chạy thành công.
- Không có `guard_warnings` sau khi chạy scoped fix.
- Re-run cùng command cho kết quả idempotent: không phát sinh thay đổi mới.
- Các mismatch ưu tiên cao đã được xử lý:
  - UC-13: chỉnh lane ownership cho decision BOH
  - UC-15: chỉnh wording và phản ánh minimum 1-star validation rõ hơn
  - UC-16: chỉnh wording + controlled rewiring để main flow gần spec hơn

## Ghi chú
- Một số lane `x` swaps và wording fixes ở các file ưu tiên đã tồn tại sẵn trước khi run; script chấp nhận các trường hợp này như no-op hợp lệ nhờ cơ chế idempotent.
- Với activity draw.io midterm, relayout thuần và spec-alignment auto-fix giờ là 2 mode khác nhau, nhưng đều đi qua cùng script canonical để giữ audit trail và consistency.
- Nếu tiếp tục sửa spec-alignment ở activity draw.io, phải thêm mapping explicit thay vì chỉnh heuristic hoặc sửa tay không kiểm soát.
