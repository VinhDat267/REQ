# Journal - 2026-05-04

## Standalone requirement-management DOCX refresh

- Refreshed `Project Thesis_Team01_Tut01_Requirement_Management.docx` from the current `Thesis.docx` Section `VI. Requirement Management`.
- Preserved only the requirement-management section content and its tables:
  - baseline control
  - use-case-level RTM
  - change management
  - prioritization / effort assessment / scalability
  - requirement risk categories and risk log
  - scope management
- Added `References Used in This Section` at the end because the refreshed Section VI contains in-text citations.
- Created `Project Thesis_Team01_Tut01_Requirement_Management_BACKUP_before_2026-05-04_refresh.docx` before overwriting the previous standalone file.
- Verification via `python-docx` showed:
  - file starts with `VI. Requirement Management`
  - `38` paragraphs
  - `7` tables
  - no Section `V`, `VII`, `VIII`, Introduction, Literature, Client, or Elicitation content
  - `0` inline shapes
  - approximately `3,502` words including table text

## Repository cleanup

- Removed obvious non-deliverable clutter from the workspace:
  - temporary thesis backup DOCX files
  - old standalone requirement-management backup DOCX
  - transient text dumps: `review_output.txt` and `temp_thesis.txt`
  - untracked one-off helper scripts created for thesis / prototype review and splitting work
- Kept primary deliverables and active work intact:
  - `Thesis.docx`
  - `Project Thesis_Team01_Tut01.docx`
  - `Project Thesis_Team01_Tut01_Requirement_Management.docx`
  - frontend prototype screens
  - tracked scripts and canonical diagram / requirement sources
- Cleanup removed `29` files, approximately `25 MB`.
- A recursive scan after cleanup found no remaining `*BACKUP*`, `*.tmp.docx`, Word lock `~$*`, `*.bak`, `review_output.txt`, or `temp_thesis.txt` files.

## Duplicate thesis removal

- Deleted stale duplicate thesis file `Project Thesis_Team01_Tut01.docx` after confirming `Thesis.docx` is the newer full thesis with the latest Client, Prototype, Requirement Management, citation, and Revision updates.
- Kept `Project Thesis_Team01_Tut01_Requirement_Management.docx` because it is the standalone Section `VI. Requirement Management` extract requested by the user.
- Verification showed only two thesis-related DOCX deliverables remain:
  - `Thesis.docx`
  - `Project Thesis_Team01_Tut01_Requirement_Management.docx`

## BRD Ver3 re-fix after source-control discard

- Reapplied the lost improvements to `Group1_Tutorial01_BRD_Ver3.docx` after the working-tree changes were accidentally discarded.
- Restored the BRD readiness polish:
  - renamed Appendix B to `Appendix B - Business Rules and Business Case`
  - inserted a `12`-rule `Business Rules Catalog`
  - kept the previous business-case material under `Business Case Summary`
  - aligned stakeholder/client identity to `Wonton Noodle Shop`
  - reconciled business-case wording with academic prototype constraints
  - removed unvalidated ROI numbers and hard-cost claims
  - removed scope-risk phrases around full inventory management, multi-wallet integration, hardware procurement, hardware vendor contracts, and tabletop ordering
  - fixed `Sysem Analyst`, version-history placeholder text, and blank project end-date value
  - removed stale `Use Case Narrative Instructions` TOC text
  - filled previously empty UC trigger / alternative / exception cells and removed blank UC rows
- Rebuilt the `TABLE OF CONTENTS` as a Word automatic TOC with page numbers and hyperlinks.
- Promoted the main BRD outline to Word heading styles and moved Appendix A `Use Case 01..26` headings to `Heading 4` so they do not make the TOC overly long.
- Later disabled DOCX open-time field refresh so Word opens the file without showing the update-field popup; TOC updates should be done manually when needed.
- Verification via `python-docx` showed:
  - `42` tables total
  - one `13 x 3` business-rules table
  - one Word `TOC \o` field
  - no temporary TOC marker
  - full `UC-01..UC-26` coverage with no missing or extra UC numbers
  - no empty UC specification cells
  - no stale stakeholder restaurant name, repo-internal phrases, or checked scope-risk phrases
  - no tracked-change or comment markup

## BRD Ver3 font normalization

- Standardized `Group1_Tutorial01_BRD_Ver3.docx` formatting from Word page 3 onward while keeping the first two pages unchanged.
- Applied `Times New Roman` size `13` to body and table text from page 3 to the end of the document.
- Reapplied larger heading sizes after the body-formatting pass:
  - TOC title: `16` bold
  - `Heading 1`: `16` bold
  - `Heading 2`: `15` bold
  - `Heading 3`: `14` bold
  - `Heading 4`: `13.5` bold
- Updated the automatic TOC once after the font change so page numbers match the new pagination.
- Kept Word open-time field refresh disabled, so the document no longer shows the update-field prompt when opened.
- Verification via Word automation showed:
  - total document length after formatting: `129` Word pages
  - updated TOC pagination
  - sampled body paragraphs from page `4+`: `Times New Roman`, size `13`
  - sampled headings: `Times New Roman`, larger heading sizes as listed above
  - no temporary TOC marker
  - no `updateFields` setting in DOCX settings

## SRS review fix after BRD closure

- Review-fixed root `SRSReport_Class01_Group01.docx` against the closed BRD.
- Synced all `26` SRS use case specification tables to the BRD for:
  - use case title
  - primary actor
  - priority
- Updated section `2.4.1 FUNCTIONAL REQUIREMENTS` so `R.1..R.26` use the same canonical UC names as the closed BRD.
- Replaced the previous bullet-only non-functional requirements section with a traceable `NFR-01..NFR-09` table aligned to the BRD, including priority, description, rationale, and related use cases.
- Tightened scope/formal language:
  - removed checked `final project`, `repository`, and `academic review` style phrases from the SRS body
  - kept Delivery, native mobile, loyalty, payroll, full accounting, and complete supplier procurement explicitly out of scope
  - kept Bank Transfer QR as manual confirmation rather than bank callback
- Applied Word heading styles to numbered SRS headings (`Heading 1..3`) so the document outline is formal.
- Verification via `python-docx` showed:
  - `26` SRS UC tables with `0` title / actor / priority diff against the closed BRD
  - `26` `R.1..R.26` headings detected and mapped to `UC-01..UC-26`
  - `9` NFR rows in the new NFR traceability table
  - no empty UC specification cells
  - no tracked-change or comment markup

## SRS heading size correction

- Corrected the SRS visual hierarchy after review feedback that the main section heading was much too large.
- Updated Word heading formatting in `SRSReport_Class01_Group01.docx`:
  - `Heading 1`: reduced from `36` pt to `16` pt
  - `Heading 2`: set to `14` pt
  - `Heading 3`: set to `13` pt
  - `Heading 4`: set to `13` pt
- Kept the change limited to heading formatting; no SRS business content was changed.
- Verification via Word automation sampled the document outline and showed:
  - `1. INTRODUCTION`: `Heading 1`, `Times New Roman`, `16` pt
  - `2. OVERALL DESCRIPTION`: `Heading 1`, `Times New Roman`, `16` pt
  - `2.1 PRODUCT PERSPECTIVE`: `Heading 2`, `Times New Roman`, `14` pt
  - `2.4.1 FUNCTIONAL REQUIREMENTS`: `Heading 3`, `Times New Roman`, `13` pt

## SRS table conversion for early sections

- Converted three early SRS sections from bullet lists into formal two-column tables:
  - `1.3 Definitions, Acronyms, and Abbreviations`
  - `2.2 SOFTWARE REQUIREMENT`
  - `2.3 HARDWARE REQUIREMENT`
- Used `Term / Acronym` and `Definition / Explanation` columns for section `1.3`.
- Used requirement-area and requirement-description columns for sections `2.2` and `2.3`.
- Kept the existing meaning and scope wording; the change was presentation / document-structure oriented.
- Verification via Word automation showed:
  - section `1.3`: one table, `45` rows including header, no bullet symbols
  - section `2.2`: one table, `8` rows including header, no bullet symbols
  - section `2.3`: one table, `6` rows including header, no bullet symbols
  - no heading-styled paragraphs remain inside those tables
