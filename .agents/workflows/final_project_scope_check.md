---
description: Verify final-project work aligns with the official final brief without overwriting the locked midterm lane by accident.
---

1. Read `Final_Project_Scope.md`, `All_Use_Cases.md`, `.agents/context/current-status.md`, `.agents/context/project-overview.md`, `.agents/context/conventions.md`, and `.agents/rules.md`.
2. Confirm the artifact being edited belongs to the repository's default `final-project` whole-system scope unless the user explicitly asked for archival midterm maintenance.
3. Confirm the work uses `Final_Project_Scope.md` plus `All_Use_Cases.md` as the primary scope reference.
4. If the artifact is `BRD final v3`, confirm it preserves the local visible lane (`UC-01..UC-16` + selected `UC-17+`) instead of forcing the original master IDs into the main narrative.
5. Confirm the change stays inside the `2026-04-17` business scope lock; if it adds business capability, stop and create/seek a scope-change decision first.
6. Confirm the narrative keeps exactly one client problem statement.
7. Confirm the relevant final-project expectations are accounted for:
   - full-version requirements and UC specifications
   - at least `10` literature / article / online references
   - elicitation notes framed by `plan / prepare / conduct / confirm`
   - `3` whole-system diagram types
   - change / risk / scope management
   - prototype evidence
   - thesis and role-based submission packaging
8. Confirm `Delivery` has not been reintroduced unless root product scope has intentionally changed.
9. Summarize gaps before making further changes.
