# Journal - 2026-04-04

## Summary
- Audited the repository against the vibe coding guide.
- Added a Codex-native root `AGENTS.md` entrypoint.
- Added a minimal `CLAUDE.md` pointer to keep agent entrypoints aligned.
- Recorded the workflow decision in `009-codex-agent-entrypoint.md`.
- Expanded `AGENTS.md` into a richer Codex bootstrap pattern based on a stronger multi-section template.
- Recorded the follow-up workflow decision in `010-expand-codex-bootstrap-pattern.md`.
- Adapted the requested AGENTS rule set to Wonton POS instead of copying Redline-specific instructions.
- Added `codex-bridge.md` and `.agents/README.md` as Codex support references.
- Rewrote workflow docs so they match the current docs-first phase and removed placeholder runtime guidance.
- Recorded the standardization decision in `011-standardize-codex-bootstrap-for-docs-first-repo.md`.
- Added a midterm-only ERD in Mermaid and PlantUML, derived from the locked 16-UC scope instead of the full 74-UC system.
- Updated repo navigation/context docs so the new ERD artifacts are discoverable in future sessions.
- Tightened the midterm ERD after review by adding item-level preparation progress to `ORDER_ITEM` and widening `NOTIFICATION` to cover both order and menu-item event contexts.
- Reworked the ERD presentation to be easier to read: one overview plus domain-focused Mermaid views, and added a separate PlantUML overview artifact.

## Notes
- Root markdown requirement documents remain the source of truth.
- `.agents/` remains the maintained working-memory layer.
- `AGENTS.md` is intentionally concise and should stay synchronized with `.agents/context/`.
- `AGENTS.md` now defines instruction priority, session-start reads, working rules, and completion rules for Codex.
- Workflow docs now describe the current repository posture instead of an imagined app runtime.
- The midterm ERD intentionally excludes promotions, delivery, full auth submodels, inventory/86'd audit tables, and other full-system modules that are outside the selected 16 UC scope.
- The ERD now allows menu-item alert notifications without forcing every notification row to reference an order.
