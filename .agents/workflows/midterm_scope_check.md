---
description: Verify an explicitly historical midterm edit stays inside the locked 16-UC package without confusing it with the active final-project scope.
---

1. Read `.agents/rules.md`, `.agents/context/conventions.md`, and `.agents/context/current-status.md`.
2. Confirm the user explicitly asked to edit a `midterm-locked` historical artifact.
3. Confirm the active scope still uses the local `UC-01..UC-16` numbering set.
4. Confirm `Delivery` was not reintroduced into active logic.
5. Confirm the final-project brief is not being used to rename or renumber the midterm package.
6. Confirm `Usecasediagramreq.drawio` remains the only legacy draw.io artifact kept at repo root.
7. Summarize any drift or inconsistencies before making further changes.
