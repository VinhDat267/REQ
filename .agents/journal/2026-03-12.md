# Journal - 2026-03-12

## Những gì đã làm
- Dựng 16 Activity Diagram canonical ở dạng PlantUML cho `UC-01..UC-16` tại `docs/diagrams/plantuml/activity/`.
- Tạo `docs/diagrams/activity/ASSIGNMENTS.md` để map đúng 2 diagram cho từng thành viên.
- Kiểm tra khả năng export PNG tự động trong môi trường hiện tại và xác nhận `plantuml` CLI chưa có sẵn.
- Rà lại cấu trúc repo sau đợt dọn dẹp.
- Xác nhận root hiện chỉ giữ các requirements markdown chính thức, thư mục làm việc cốt lõi và `Usecasediagramreq.drawio` như artifact legacy canonical.
- Xác nhận 2 file DOCX tham chiếu đã nằm trong `docs/reference/`:
  - `S2026_REQ_Midterm_exam_requirement.docx`
  - `Wonton_POS_Use_Case_Specifications_EN_Final.docx`
- Xóa file template DOCX cũ không còn dùng ở root.
- Tạo sẵn khung thư mục `docs/diagrams/drawio/erd/` và `docs/diagrams/drawio/use-case/` để tổ chức thêm các file draw.io về sau.
- Cập nhật lại `.agents/context/current-status.md`, `.agents/context/project-overview.md`, `.agents/context/architecture.md`, `.agents/context/conventions.md` và `.agents/rules.md` để phản ánh trạng thái repo mới.

## Kết quả
- Repo sạch hơn, ít artefact legacy rải ở root hơn.
- `Usecasediagramreq.drawio` tiếp tục là ngoại lệ legacy duy nhất được giữ ở root theo convention đã chốt.
- Agent brain đã phản ánh đúng quy ước mới: tài liệu tham chiếu vào `docs/reference/`, draw.io mới vào `docs/diagrams/drawio/`, root ưu tiên giữ source-of-truth markdown.
- [2026-03-12] Fixed all 16 Activity Diagrams to UML 2.x standards:
  - Added explicit merge nodes after decision branches
  - Replaced multiple `stop` nodes with `detach` for flow finals
  - Reintroduced swimlanes for visual actor separation
  - Validated syntax and rendering with PlantUML v1.2026.2

## Ghi chú
- `docs/diagrams/drawio/erd/` và `docs/diagrams/drawio/use-case/` hiện là khung thư mục, có thể còn rỗng cho tới khi thêm diagram mới.
- `docs/diagrams/drawio/activity/` hiện đã chứa bộ `.drawio` activity cho `UC-01..UC-16`; note cũ về thư mục chỉ là khung không còn đúng nữa.
- `docs/diagrams/exports/activity/` đã được tạo khung nhưng chưa export PNG vì môi trường hiện tại chưa có `plantuml` CLI.
- `README.md` đã được cập nhật để phản ánh trạng thái hiện tại của project.
