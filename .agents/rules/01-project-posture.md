# Project Posture

- This repository is documentation-first and requirements-first.
- Root markdown requirement files are the source of truth.
- `.agents/` is the maintained project memory layer, not a second source of truth.
- `src/` and `tests/` are placeholders in the current phase.
