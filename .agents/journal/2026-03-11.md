# Journal - 2026-03-11

## Những gì đã làm
- Review và chỉ ra các mâu thuẫn giữa BRD, Business Rules và Use Cases.
- Sửa trực tiếp bộ requirements để đồng bộ lại toàn hệ thống.
- Chốt các rule nghiệp vụ quan trọng:
  - 3 service models: `Dine-in`, `Takeaway`, `Pickup`
  - `Delivery` ngoài scope
  - `Takeaway` và `Pickup` phải trả trước
  - `Pickup` auto-accept sau khi kiểm tra slot/capacity
  - Guest tracking bằng `Mã đơn + SĐT`
  - Tách `order_status` và `payment_status`
- Đồng bộ lại các file VN và EN:
  - BRD
  - Business Rules
  - All Use Cases
  - UC Specifications Part 1/2
- Sửa lỗi mojibake/encoding ở bộ tài liệu chính.
- Chuẩn hóa bảng Functional Requirements và Non-functional Requirements về cùng schema 5 cột: `REQ#`, `PRIORITY`, `DESCRIPTION`, `RATIONALE`, `USE CASE`.
- Đồng bộ lại bảng phân chia 16 UC sau thay đổi assignment:
  - Lê Thành Đạt: `UC-15` + `UC-13`
  - Nguyễn Thiện Hiếu: `UC-16` + `UC-11`
- Ở một bước trung gian, đã từng bổ sung Nguyễn Thiện Hiếu vào `Document Resources` của BRD Việt và Anh; về trạng thái cuối, mục này đã được chuẩn hóa lại còn 1 stakeholder duy nhất là `Nguyen Quang Anh`.
- Thu BRD midterm về đúng phạm vi 16 UC được chọn; Use Case Diagram tổng chỉ vẽ tập UC này thay vì toàn bộ 74 UC.
- Chỉnh lại BRD và UC Specifications để bộ midterm dùng mã UC cục bộ `UC-01` đến `UC-16`, đồng thời giữ cột `UC Gốc` để map về hệ 74 UC.
- Refresh lại `.agents/context/` để mọi tham chiếu mặc định dùng numbering cục bộ `UC-01..UC-16` thay vì numbering gốc rời rạc.
- Làm sạch `.agents/specs/srs.md` để không còn kéo logic cũ như `Delivery`.
- Dựng 2 sơ đồ Use Case Diagram cho bài midterm ở dạng PlantUML:
  - `docs/diagrams/customer-midterm-use-case-diagram.puml`
  - `docs/diagrams/admin-operations-midterm-use-case-diagram.puml`
- Lưu editable sources tương ứng tại:
  - `docs/diagrams/drawio/use-case/customer-midterm-use-case-diagram.drawio`
  - `docs/diagrams/drawio/use-case/admin-operations-midterm-use-case-diagram.drawio`
- Bổ sung các quan hệ `<<include>>` và `<<extend>>` vào sơ đồ PlantUML, đồng thời tách một nhóm supporting UCs để vừa đáp ứng yêu cầu đề bài vừa không phá phạm vi 16 UC midterm.
- Đối chiếu lại BRD, Business Rules, All Use Cases và UC Specifications theo hướng "diagram-ready" để khóa actor, boundary và external systems phục vụ cho lần vẽ tiếp theo.
- Bổ sung external actor `Payment Gateway (Sandbox)` vào sơ đồ `Admin / Operations` vì `UC-10` cũng có nhánh online/sandbox theo BRD và UC spec.
- Đọc sâu lại `All_Use_Cases.md`, `BRD_Ver0.md`, `UC_Specifications_Part1.md`, `UC_Specifications_Part2.md`, `Business_Rules.md` và khóa lại `docs/diagrams/use-case-diagram-basis.md` thành bản chuẩn cho các lượt chỉnh/vẽ tiếp theo.
- Đồng bộ lại 2 sơ đồ hiện có ở cả PlantUML và draw.io để title, boundary label và scope note khớp hoàn toàn với `docs/diagrams/use-case-diagram-basis.md`.
- Tối ưu lại bố cục của 2 file PlantUML bằng `nodesep`, `ranksep`, `linetype ortho` và hidden links để khoảng cách actor/use case hợp lý hơn mà không đổi nội dung nghiệp vụ.
- Review 2 sơ đồ như một BA và chỉnh lại PlantUML theo hướng `BA-ready / submission-ready`: đổi boundary về đúng subject system (`Client WebApp` / `Admin WebApp`), bỏ `Dang nhap Admin` ra khỏi use case diagram và xử lý như precondition chung, đồng thời chuẩn hóa lại các quan hệ `extend/include/generalization` ở sơ đồ khách.
- Cập nhật lại `docs/diagrams/use-case-diagram-basis.md` theo model BA-ready.
- Sync tiếp 2 file `.drawio` để boundary, actor model, supporting UC và quan hệ UML khớp với PlantUML BA-ready; loại bỏ luôn model cũ dùng `Customer Boundary`, `Admin / Operations Boundary` và `Dang nhap Admin` như một use case riêng.
- Theo yêu cầu submission, bổ sung lại `Dang ky tai khoan`, `Dang nhap`, `Dang nhap Admin` vào 2 sơ đồ dưới dạng supporting authentication UCs ngoài 16 UC chính, thay vì nhét chúng vào tập selected midterm UCs.
- Đọc `vibe_coding_guide.md` và xác nhận project đang dùng đúng mô hình `.agents/` làm “brain”.
- Refresh lại các file context, rules và decision log trong `.agents/`.
- Đồng bộ lại brain để thay các tham chiếu diagram cũ bằng artefact thực tế đang có trong repo.

## Kết quả
- Bộ requirements hiện nhất quán giữa các tài liệu chính.
- Bộ não `.agents/` phản ánh đúng trạng thái mới nhất của project.
- Context cho các phiên sau không còn phụ thuộc vào việc nhắc lại thủ công toàn bộ thay đổi.
- BRD Việt/Anh đã được formal hóa lại FR/NFR theo style `The system shall ...`.
- `Usecasediagramreq.drawio` đã được sửa xong, sync với BRD midterm hiện tại và đủ điều kiện chốt cho scope giữa kỳ.
- `midterm-16-use-case-overview.puml` đã được sửa nốt các relation còn lệch để khớp với BRD, UC specs và draw.io.

## Ghi chú
- `README.md` vẫn là tài liệu phụ, chưa phải source of truth.
- `src/` vẫn chưa có code thực tế; project đang ở phase tài liệu và chuẩn bị diagrams.
- Với use case diagram midterm: ưu tiên logic theo BRD + PlantUML canonical; file draw.io ở root là bản đã sync để review/submission.

## Việc tiếp theo
1. Vẽ Activity Diagram và Process Flow Diagram cho 16 UC đã phân công.
2. Cập nhật `README.md`.
3. Nếu chuyển sang implementation, chốt tech stack và roadmap code.

